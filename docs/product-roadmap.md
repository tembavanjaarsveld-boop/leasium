# Leasium Product Roadmap

Last updated: 2026-05-31

Leasium is being shaped around a simple operating promise: documents should turn into work with as little re-keying as possible. Drop the contract, lease, invoice, certificate, or guarantee; Leasium reads it, matches it to the portfolio, suggests the changed fields, and only asks a human to review and approve.

Design-facing changes require Remba UX sign-off. See [design-governance.md](design-governance.md) and the brand/frontend source of truth in [leasium-codex-design-source-of-truth.md](leasium-codex-design-source-of-truth.md).

## Built

- [x] Multi-entity foundation with organisation, entity, property, tenancy unit, tenant, lease, and obligation registers.
- [x] Entity-scoped roles, audit logging, dev auth, and Clerk adapter boundary.
- [x] Leasium rebrand with basic logo mark, palette, API app name, and web metadata.
- [x] Lease upload intake for PDF, DOCX, text, and Markdown files.
- [x] AI lease extraction into editable property, unit, tenant, lease, and obligation data.
- [x] Lease intake apply flow that creates register records only after review.
- [x] Friendly stale entity/property recovery in the dashboard.
- [x] Tenant onboarding links with public web form and tenant record update on submit.
- [x] Billing-readiness slice: charge rules, rent roll API, readiness blockers, and dashboard panel.
- [x] Lease intake matching: reuse existing property, unit, and tenant records from the review step.
- [x] Lease intake apply validation for missing critical fields and overlapping unit lease ranges.
- [x] Asynchronous lease intake extraction with upload returning immediately and review polling for completion.
- [x] Tenant onboarding cancellation so stale public links can be retired and recreated.
- [x] Navigation split: dashboard home, property operations page, and tenant register page.
- [x] Tenant workspace upgrade: onboarding command center, status filters, prioritized queue, and tenant profile route.
- [x] Property workspace task-zone split: `/properties` now separates Portfolio, Operations, Billing, and Documents, and property create/edit opens in a focused overlay instead of an always-visible side rail.
- [x] Billing Readiness task-zone split: `/billing-readiness` now separates Readiness, Billing drafts, Invoice prep, and Delivery & payments, with the blocker action queue and no-posting/no-Xero guardrails kept visible.
- [x] Managed onboarding backend workflow: expiry, resend, cancel reason, review, apply, and audit-friendly timestamps.
- [x] Public tenant onboarding refresh: property/unit context, guided sections, notes field, clearer reset/submit states, and next-step reassurance.
- [x] Stable hosted demo path: Vercel frontend, Render API, Neon Postgres, CORS, seeded data, and no temporary tunnel dependency.
- [x] MVP tenant document storage: upload, list, download, soft-delete, and tenant profile UI for insurance, guarantees, leases, invoices, and onboarding files.
- [x] Tenant onboarding uploads and review-first apply: public token document uploads, staff review documents, labelled submitted details, and no profile mutation before Apply.
- [x] Smart Intake Inbox first slice: dashboard drag/drop, stored source document, generic AI classification/extraction envelope, review status queue, summary groups, confidence, warnings, and no automatic record mutation.
- [x] Focused Smart Intake review surface: approve/edit/ignore item decisions, visible confidence and source hints, ignored-item filtering, insurance apply target matching, and explicit change summary before Apply.
- [x] Smart Intake document-to-task apply: approved certificates, compliance docs, bank guarantees, and notices create scoped obligations/tasks, link the source document, and show a calm post-apply outcome.
- [x] Smart Intake lease apply: reviewed lease documents can create/reuse property, unit, tenant, lease, source document links, and lease obligations through an Apply plan that keeps humans in control.
- [x] Smart Intake billing prep: reviewed invoice/admin documents prepare source-linked billing review tasks with money context and clear no-invoice/no-Xero-sync guardrails.
- [x] Smart Intake acquisition apply: reviewed purchase contracts can create or link property records, optionally create/link reviewed units, attach the source document, and create milestone tasks without posting financials.
- [x] Smart Intake acquisition tenancy schedule v1: purchase contracts can extract reviewed schedule rows, create/link multiple units, preserve tenant/rent/lease facts on unit provenance metadata, create pending tenant/lease records for complete reviewed rows after Apply, generate lease expiry/rent review/option/security tasks, and seed draft-marked base rent/outgoings charge rules from reviewed rows.
- [x] Smart Intake acquisition tenancy schedule v2: reviewed rows can now capture parking, storage, utilities, promotion levy, and other charge amounts; Apply creates draft-marked non-rent charge rules, skips invalid rows with clearer blockers, and the post-apply outcome surfaces pending lease IDs, draft charge detail, and skipped schedule rows.
- [x] Smart Intake billing drafts v1: reviewed invoice/admin documents now create source-linked draft billing documents and lines, support reviewed draft status updates, and preserve the no-posting/no-Xero-sync guardrail.
- [x] Billing draft review surface: Billing Readiness now lists source-linked Smart Intake billing drafts with status, due date, amount, source context, and approve/void actions that still do not post invoices, email tenants, or sync to Xero.
- [x] Invoice draft staging v1: approved billing drafts can create internal source-linked invoice drafts and lines, surface recipient/readiness blockers in Billing Readiness, and preserve no-PDF/no-email/no-Xero-sync guardrails.
- [x] Invoice draft delivery preparation v1: internal invoice drafts can prepare an approval-safe invoice preview and tenant email draft metadata, show delivery blockers, move complete drafts to ready for approval, and keep PDF storage, tenant sending, and Xero sync explicitly off.
- [x] Invoice generation and delivery v1: internal invoice drafts now store source-linked PDF artifacts, keep branded email draft metadata, record manual tenant delivery receipts after approval, maintain approval-safe posting preparation, and track unpaid/part-paid/paid status with no Xero sync.
- [x] Smart Intake naming pass: global navigation and workspace copy now reflect the broader document-first workflow instead of lease-only inbox wording.
- [x] Smart Intake task queue: open document reviews, match issues, processing states, and failed reads now appear in Tasks with a dedicated filter and deep links back to Smart Intake.
- [x] Smart Intake applied outcome counts: the applied outcome panel now surfaces returned billing draft, pending lease, and draft charge counts from the backend apply result.
- [x] Tenant onboarding delivery foundation: Twilio SendGrid email and Twilio SMS delivery are wired into create/resend with channel status, audit logs, and safe skipped states when a channel is not configured.
- [x] Tenant onboarding reminders and receipts v1: reminder schedules, manual due-reminder runner, SendGrid/Twilio receipt ingestion, signed Twilio status callback verification when `TWILIO_AUTH_TOKEN` is configured, delivery timeline, and contact-fix actions.
- [x] Tenant onboarding delivery polish v1: outbound delivery stores brand/template metadata, reminder schedules can be edited through the API, expiry reminders are scheduled before link expiry, and contact/configuration recovery hints stay visible.
- [x] Tenant detail deepening v1: tenant detail now has property/unit/lease context, richer activity history, safer document provenance/actions, reviewed onboarding change history, and public-fact enrichment suggestions.
- [x] Public AI enrichment v1: property and tenant records can request safe public facts such as ABN, suburb/state/postcode, registered names, and registered addresses from trusted web sources, show citation/confidence, and apply only reviewed suggestions.
- [x] Property ownership and billing identity: owner/trust/split profile fields, quiet property-level chips, collapsible setup, billing-readiness blockers, and Smart Intake extraction/apply support.
- [x] Smart Intake property provenance v1: purchase contract applies now store source citations, before/after property field changes, apply history, and property audit logs.
- [x] Smart Intake property provenance UI: purchase-contract apply outcomes show property before/after changes, and the Property workspace surfaces stored field citations, latest apply history, confidence, and deep links back to the source intake.
- [x] Temporary private-beta access gate: Vercel frontend supports an env-controlled password screen while tenant onboarding links remain publicly reachable.
- [x] Operator security and settings arena v1: Settings now has Security, Organisation, and Xero sections; `/api/v1/me` and `/api/v1/security/workspace` expose the current operator, organisation, roles, auth boundary, and members; owner/admin users can add operator access records, adjust entity roles, and activate/deactivate teammates over the existing user/entity-role foundation.
- [x] Operator invite and Clerk linking v1: owner/admin users can send SendGrid-backed operator invite emails, invite links are stored as hashed one-time tokens with expiry/status, `/accept-invite` links a signed-in Clerk user to the existing Leasium operator record, and backend Clerk mode can verify sessions against JWKS before mapping the Clerk subject to `app_user.auth_provider_id`.
- [x] First workspace setup foundation: clean Clerk-backed databases can create the first organisation, entity, and owner operator through a reviewable `/setup` flow, and Clerk mode now fails closed unless JWKS verification is configured or an explicit legacy test flag is enabled.
- [x] Operator workspace sign-in guard: when Clerk server and publishable keys are configured, middleware redirects signed-out protected workspace requests to operator sign-in before dashboard/API loading; the client shell also shows a friendly sign-in required state, while public onboarding, setup, invite, and access-gate routes remain open.
- [x] Production operator-login rollout: the first operator invite/account has been linked, Vercel protected routes redirect signed-out users to Clerk sign-in, tenant onboarding remains public, and the Render API now enforces Clerk bearer tokens for protected workspace APIs.
- [x] Xero readiness and mapping v1: Settings now surfaces entity connection state, contact readiness, chart/account mapping gaps, tax mapping gaps, approved invoice sync queues, and payment reconciliation counts, with suggested charge-rule mappings and no live Xero posting.
- [x] Xero provider connection foundation v1: Settings can start a Xero OAuth connection, the API stores encrypted provider tokens in a per-entity connection record, records the selected Xero organisation, and can run a provider-backed contact sync preview that suggests tenant/property matches without applying mappings, posting invoices, or reconciling payments.
- [x] Reviewed local Xero contact mapping apply: provider contact preview suggestions can now be applied locally after review through an explicit apply-preview contract, with skipped mappings and no invoice posting, tenant email, or payment reconciliation side effects. Pending Remba review.
- [x] Provider-backed Xero chart/tax validation preview: `/api/v1/xero/chart-tax/validate-preview/{entity_id}` can validate local charge-rule account and tax mappings against provider chart/tax settings, returning checked-rule totals, per-rule results, and guardrails without invoice posting or payment reconciliation. Pending Remba review.
- [x] Provider-backed Xero invoice posting preview: `/api/v1/xero/invoices/posting-preview/{entity_id}` prepares reviewed Xero draft invoice payloads and blocker lists for approved unsynced invoice drafts, with provider contact/chart/tax checks and no invoice posting, tenant email, Xero mutation, or payment reconciliation. Pending Remba review.
- [x] Owner statement preview v1: `/statements` now has an owner-selectable finance review pack with owner contact context, monthly totals, property lines, copyable review summary, and a print/save-PDF action while keeping owner dispatch as an explicit future step. Pending Remba review.
- [x] Owner statement dispatch review v1: the statement preview now includes a recipient-readiness and owner-facing email draft panel with copy-to-clipboard support plus a local `owner-statement-dispatch-draft-{month}-{owner}.txt` download for the selected owner, and the dispatch approval queue has a local `owner-statement-dispatch-review-{month}.csv` download covering owner, status, recipient, subject, invoice/property counts, outstanding amount, approval runway, and no-send/no-provider guardrails. This prepares for a later reviewed SendGrid dispatch path without sending email, downloading PDF packs, writing Xero data, reconciling payments, or writing provider history. Pending Remba review.
- [x] Owner statement invoice evidence UI v1: the selected owner statement preview now surfaces invoice-level evidence rows with invoice number/title, property, due date, amount, paid/outstanding, payment status, and local/Xero/reconciliation evidence source, plus a local invoice-evidence CSV download for the selected owner/month that includes the same source trail. This matches the backend JSON/PDF/ZIP evidence artifacts without sending owner email or mutating providers. Pending Remba review.
- [~] Owner portal read-only preview v1: new operator-preview endpoint `GET /api/v1/owner-portal/{owner_id}` and route `/owner-portal/[ownerId]` show one owner identity, linked properties/splits, monthly statement totals, statement property lines, and explicit no-send/no-provider guardrails. This is not public owner login yet; true owner account/token auth remains the next portal slice. Pending deploy verification and Remba review.
- [x] Insights overview v1: the Insights workspace now has a read-only backend overview for portfolio health, live exceptions, automation activity, billing risk, owner/entity snapshots, and a local `insights-review-packet-{as_of}.csv` export covering live exceptions, automation activity, finance/accounting readiness, owner/entity gaps, lease events, snapshot history, and no-provider-mutation guardrails. No record mutation, invoice posting, Xero sync, provider refresh, SendGrid/Twilio send, payment reconciliation apply, billing draft generation, provider dispatch, or provider-history write runs from the overview export.
- [x] Shareable Insights snapshots v1: operators can freeze owner, finance, and lease-event snapshots from the live Insights overview into revocable public links with hashed tokens, expiry, public read-only rendering, and no live portfolio mutation.
- [x] Spreadsheet portfolio import dry-run v1: `.xlsx` source-of-truth workbooks can be parsed into a no-mutation import plan for properties, units, tenants, leases, charge rules, obligations, and operational tasks, with row-level blockers/warnings and feature-candidate flags for vendors, legal entities, head leases, security originals, arrears, and issue/action queues.
- [x] Spreadsheet portfolio Smart Intake review/apply v1: spreadsheet import plans now surface reviewable row actions in Smart Intake, require explicit approved action IDs before Apply, and store workbook filename, sheet, row, action ID, source hint, confidence, and before/after provenance on created/updated records. Pending Remba review.
- [x] Spreadsheet import plan durability v1: dry-runs now persist a server-side review plan ID, Smart Intake Apply can use the stored plan instead of trusting browser-sent action rows, and applied plans keep the approved/ignored IDs plus apply result for audit. New migration: `20260521_0020_register_import_plans`.
- [x] Spreadsheet import review controls v1: Smart Intake and the focused spreadsheet page now show approve/review/ignored/blocked counts, bulk approve-recommended and ignore-all controls, and multi-field change previews before Apply. Pending Remba review.
- [x] Spreadsheet migration template download v1: `/api/v1/register-imports/template` returns an authenticated `.xlsx` migration workbook with instructions and preset tabs for entities, properties, tenancies, charge rules, bonds, dates, vendors, arrears, issue queues, and action queues. Smart Intake and the focused spreadsheet page now expose a `Download template` action before upload, preserving dry-run/no-mutation review-first import behaviour. Verified with backend import/template tests and spreadsheet smoke. Pending Remba review.
- [x] One-off SKJ portfolio workbook production import: the current source-of-truth workbook has been loaded into the hosted register with real properties, units, tenants, leases, charge rules, and obligations, while demo seed rows were archived.
- [x] Post-import Portfolio QA workspace v1: `/portfolio-qa` brings together missing-field checks, tenant contact cleanup, source/import history, batch onboarding readiness, and internal billing draft preparation from the imported portfolio. Portfolio QA now has a primary navigation entry for pre-production cleanup, with final IA still pending Remba review.
- [x] Charge-rule billing draft batch v1: reviewed rent/outgoings charge rules can generate source-linked internal billing drafts and line items with idempotent same-period reuse, synthetic prep source documents, and explicit no-PDF/no-email/no-Xero guardrails.
- [x] Xero posting approval and draft creation v1: approved invoice drafts can receive explicit local Xero posting approval, then create provider-backed Xero draft invoices idempotently when credentials and preview validation pass; unconfigured/provider-missing states are safe skips, and no Xero write runs without approval.
- [x] Xero payment reconciliation v1: provider/imported payment statuses can be previewed and applied to local invoice metadata with idempotency keys, preserving review/audit history and avoiding duplicate reconciliation.
- [x] Xero payment reconciliation review surface v1: Settings now previews provider payment statuses, shows current/proposed local invoice payment metadata, applies a fresh provider pull into Leasium payment metadata only, and keeps Xero/bank mutation guardrails visible. Pending Remba review.
- [x] Xero operator approval UI v1: Settings now turns the provider posting preview into an explicit operator workflow with per-invoice Xero approval/revoke actions, separate batch draft creation, created/skipped/blocked/failed result panels, and guardrails that keep tenant email and payment reconciliation separate. Pending Remba review.
- [x] Provider-backed invoice email delivery v1: approved internal invoice drafts can send the prepared invoice email through SendGrid when configured, attach the stored PDF artifact, record queued/skipped/failed provider receipts, and keep Xero sync as a separate approval.
- [x] Provider invoice dispatch v1: approved invoice drafts can run an explicit combined provider action that creates or reuses the Xero DRAFT first, sends or reuses the SendGrid invoice email second, preserves Xero sync state across email delivery, and leaves payment reconciliation separate.
- [x] Provider dispatch receipts and recovery v1: Xero draft-create/dispatch attempts now persist provider receipts, failed attempts keep retry-safe approval state, dispatch responses include next-action hints, Billing Readiness exposes per-invoice Dispatch/Retry actions, and SendGrid invoice webhooks can record delivered/opened/failed email receipts without changing Xero sync state. Pending Remba review.
- [x] Billing provider history filters v1: Billing Readiness Delivery & payments now filters approved invoice drafts by all/needs action/ready to dispatch/complete/unpaid and shows per-invoice provider history for Xero dispatch attempts plus local payment reconciliation receipts. Pending Remba review.
- [x] Xero sync exception queue v1: Settings now has a read-only local exception queue for connection/mapping blockers, approved-unsynced invoice drafts, failed Xero provider receipts, and payment reconciliation candidates, plus local copy/download review exports from the already-loaded queue rows, with guardrails confirming the queue does not refresh tokens or mutate Xero. Pending Remba review.
- [x] Xero sync exception guided actions v1: Settings exception rows now show explicit operator actions for connection, mapping, invoice posting review, provider delivery recovery, and payment reconciliation, reusing the existing review-first Xero/charge-rule flows instead of adding hidden background sync. Pending Remba review.
- [x] Xero-to-Billing invoice handoff links v1: Settings posting preview, draft creation, payment reconciliation, and provider exception actions now deep-link to the exact Billing Readiness invoice/filter while keeping Xero approval, dispatch, and reconciliation as separate reviewed steps. Pending Remba review.
- [x] Xero payment reconciliation evidence v1: payment preview/apply now returns match method, confidence, amount delta, bank/reference evidence, and no-bank-feed-mutation guardrails; applied receipts store that evidence for Billing Readiness provider history without creating or editing Xero bank transactions. Pending Remba review.
- [x] Xero accounting freshness snapshot v1: `/xero/status` now includes local accounting freshness metadata, stale/missing payment-reconciliation cues for open Xero-linked invoices, and no-provider-mutation guardrails; Settings, Insights, and public finance snapshots render that readiness state. Settings now also turns the freshness state into a compact next-step card for blockers, approved unsynced drafts, Xero-linked payment review, warnings, or month-end readiness, and can copy a local freshness packet or download `xero-accounting-freshness.csv` from the already-loaded snapshot. Pending Remba review.
- [x] Xero connection diagnostics and callback feedback v1: `/xero/connection-diagnostics` reports local config, role, scope, provider setup preflight, expected redirect URI, required scopes, and connection readiness without refreshing tokens or calling Xero; Settings Xero shows OAuth success/error feedback, local readiness rows with local blocked/ready reasons, local `xero-connection-diagnostics.csv`, copyable diagnostics packet, and `xero-connection-diagnostics.txt` exports, the provider setup preflight panel, copy/download setup packet actions for finance/Render/Xero configuration, and disables provider actions when diagnostics say a capability is blocked. Live Xero provider-console validation remains external.
- [x] Billing Readiness Xero freshness cues v1: Delivery & payments now imports the local `/xero/status` accounting freshness snapshot, shows a no-provider-mutation accounting strip, and flags Xero-linked unpaid invoice rows with `Payment check missing` or `Reconciliation stale` plus a `Review payments` handoff back to Settings. Settings fresh state now reads `Reconciliation current` instead of stale-window admin copy.
- [x] Billing Readiness month-end finance checklist v1: Delivery & payments now has a compact checklist for Accounting setup, Invoice approvals, Provider dispatch, Payment reconciliation, and Month-end pack readiness, derived from approved invoice rows and local Xero freshness. The Month-end handoff panel can copy a finance packet or download `billing-month-end-handoff-{month}.csv` with approved invoice, dispatch, payment review, owner statement, missing-recipient, and guardrail rows from the already-built handoff state. It gives operators a single pre-reporting scan before owner statements or accounting reports go out, without creating Xero drafts, previewing/applying payment reconciliation, sending tenant/owner email, generating billing drafts, dispatching invoices, refreshing providers, or mutating provider history.
- [x] Owner statements Billing handoff v1: the Billing Readiness month-end pack action now opens `/statements` with the selected entity, invoice month, and close status, and Owner statements shows a statement-pack readiness panel for ready/incomplete/unpaid/blocked states using local invoice, statement, and Xero freshness data. Billing Readiness now also checks the owner-statement roll-up directly in the month-end checklist, including missing owner billing emails before dispatch review.
- [~] Owner statement PDF export v1: `/api/v1/owners/statements/pdf` now returns a review-only PDF for a selected owner/month, and `/api/v1/owners/statements/pdf-pack` returns a ZIP containing every owner PDF plus a review-only README, accountant manifest CSV, and invoice-evidence CSV for the month. Long property, invoice, Xero, bank reference, and reconciliation evidence rows wrap across lines/pages so accountant review packs stay readable. The Statements page offers authenticated per-owner PDF download, accountant-pack download, finance-checklist CSV download, signoff CSV download, dispatch-review CSV download, dispatch-draft text download, print/save-PDF, and copy-dispatch actions. The generated pack is local to Leasium and does not send owner email, post to Xero, attach files to providers, reconcile payments, or mutate delivery history. Pending Remba/accountant review of statement formatting before owner dispatch is wired.
- [x] Tenant portal self-service v1: `/tenant-portal/[token]` exposes token-scoped tenant onboarding status, lease context, approved invoices/payment summary, document download/upload, compliance status, and notification preferences without operator-only metadata. True tenant identity-provider auth remains future work.
- [~] Tenant onboarding simplification v1: onboarding stays account-first, but the invite claim gate now uses prefilled Clerk email-code sign-in/sign-up, the confirm-details form has 4 required fields plus optional disclosure, the tenant checklist is reduced to confirm/docs → review → sign, `reviewed` is hidden from tenants, operator review/apply is combined into **Approve & apply**, and lease signing is split into `/tenant-portal/lease` plus `/tenant-portal/[token]/lease` with a post-approval **Send lease pack** action. The tenant detail workflow now shows invite → tenant details → approval → lease file → send pack → sign progress, and operators can upload a custom lease file against the specific onboarding before sending the pack. The full portal stays locked until the lease is signed, while post-approval lease Q&A remains answerable by operators. Pending Clerk-enabled smoke verification and Remba review.
- [x] Maintenance work orders v1: migration-backed work orders now track tenant/property/lease scope, priority, status, contractor assignment, approvals, photos/documents, invoice/source references, soft delete, and audit history.
- [x] Arrears and credit-control v1: migration-backed arrears cases now track aged balances, reminder cadence, dispute status, promise-to-pay notes, escalation queue/status, soft delete, and audit history.
- [x] Operations workspace v1: the former Tasks entry now opens `/operations`, preserving the triage queue while adding Maintenance and Arrears tabs, KPI cards, quick-create panels, approval/start/complete actions for work orders, and reminder/escalation/resolve actions for arrears. The queue now also has a local `operations-work-queue-review.csv` export for visible work items, assignee/notification state, follow-up and escalation cues, and no-provider/no-mutation guardrails from already-loaded queue data. Pending Remba review.
- [x] Tenant portal maintenance requests v1: token-scoped tenants can submit maintenance requests from `/tenant-portal/[token]`, see prior portal-submitted requests, attach scoped tenant documents through the API, and create linked Operations work orders without exposing operator-only fields. Pending Remba review.
- [x] Operations maintenance detail v1: maintenance work orders now keep compact activity history, expose quote/approval/contractor/invoice context in an expandable Operations detail panel, link approved invoice drafts, and show portal-safe maintenance request history back to tenants. Pending Remba review.
- [x] Tenant portal account foundation v1: tenants can claim a durable portal account with a signed bearer identity plus an existing portal token, then use bearer-only `/tenant-portal/account/session` for the same scoped self-service data while existing public token links remain available. New migration: `20260520_0019_tenant_portal_accounts`.
- [x] Tenant portal account UI v1: `/tenant-portal/[token]` now shows a Clerk-backed Account Access panel, lets signed-in tenants link the portal once, reloads matching tenants through the account session, and uses bearer auth for account-scoped maintenance requests, document uploads, and notification preferences while preserving token fallback. Pending Remba review.
- [x] Tenant portal maintenance photo upload v1: tenants can attach an inline photo while submitting a maintenance request; the portal stores the photo as a tenant document first, sends its source-linked document ID with the request, shows attached file counts in the portal history, and supports both token and account-scoped auth paths. Pending Remba review.
- [x] Tenant portal account-only entry v1: linked tenants can open `/tenant-portal` or `/tenant-portal/account` without the original token URL, sign in through the tenant account boundary, load the same scoped portal data, and download account-scoped documents through bearer-backed blob downloads. Pending Remba review.
- [x] Tenant portal notification preference receipts v1: saving notification preferences now returns a visible timestamped receipt with the resulting preferred channel, while preserving the no-message-sent guardrail and account/token auth paths. Pending Remba review.
- [x] Tenant portal document provenance/actions v1: tenant-facing document rows now show filename, category, size, source, received timestamp, notes, and an explicit download action for both token and account-scoped portal sessions. Pending Remba review.
- [x] Tenant portal account lifecycle guidance v1: signed-in tenant accounts now have a bearer-backed lifecycle status check, clearer unlinked/revoked/wrong-tenant recovery copy, durable-access guidance after invite expiry, and smoke coverage for relink guidance. Pending Remba review.
- [x] Tenant portal operator access controls v1: tenant detail now lists linked tenant portal accounts, lets operators revoke blocked access or unlink for clean relinking, stores reason/audit metadata, and keeps the unlink path relink-safe. Pending Remba review.
- [x] Tenant portal account recovery receipts v1: operator revoke/unlink/restore actions now keep compact recovery receipts, tenant detail shows the latest staff recovery receipt, revoked accounts can be restored without a fresh link, and tenant account status confirms unlinked/restored recovery state. Pending Remba review.
- [x] Tenant portal fresh-link recovery v1: staff can rotate and resend/copy a fresh portal link for sent onboarding rows, expired tokens stop opening, completed public onboarding can no longer be resubmitted or mutated from the public link, and tenant/profile/property/Portfolio QA surfaces avoid copying stale links. Pending Remba review.
- [x] Operations work-order detail route v1: maintenance rows now open a dedicated work-order page with status, contractor, quote approval, invoice linking, activity history, linked evidence downloads, and contractor quote document upload through the existing document store. Pending Remba review.
- [x] Operations work-order comments v1: the dedicated work-order page now supports structured internal/contractor/tenant comments, stores them in work-order metadata with activity history and audit receipts, and keeps them visible in the work-order timeline. Pending Remba review.
- [x] Tenant-safe maintenance history v1: tenant portal maintenance timelines now show tenant-submitted events and explicitly tenant-visible operator comments only, hiding internal and contractor-only activity while keeping operator work-order history intact. Pending Remba review.
- [x] Maintenance invoice handoff polish v1: work-order detail now renders quote/evidence metadata as a clearer preview list and shows linked invoice draft status, payment state, delivery readiness, preview/PDF actions, and existing prepare/approve controls without adding new persistence. Pending Remba review.
- [x] UX command-center IA pass v1: primary navigation now groups the app around Dashboard, Smart Intake, Portfolio, Work, Billing, Insights, and Settings; Portfolio QA remains discoverable through command search, Dashboard has a ranked daily command center across reviews/billing/onboarding/operations, Billing Readiness uses plain-English billing-run tabs, the Property workspace avoids duplicating the global Work label, and property source history now uses a reusable evidence/source-trail pattern. Pending Remba review.
- [x] Evidence source-trail rollout v2: Smart Intake acquisition apply outcomes and tenant detail reviewed-change history now use the reusable evidence/source-trail pattern for source document, source location, confidence, before/after changes, and audit history. Pending Remba review.
- [x] Operations-to-Billing handoff v1: linked maintenance invoice cards now explain whether Operations or Billing owns the next step, deep-link to the right Billing Readiness invoice tab/filter, and Billing Readiness highlights maintenance-linked invoice rows with a return path to the work order. Pending Remba review.
- [x] Contractor communication delivery v1: maintenance work-order detail can send an explicit SendGrid-backed contractor update from the Contractor card, store send status/body/subject/provider message ID in work-order metadata, keep queued messages as contractor-visible comments, and ingest SendGrid delivered/opened/failed receipts without exposing contractor-only activity to tenants. Pending Remba review.
- [x] Contractor SMS delivery v1: maintenance work-order detail can send an explicit Twilio-backed contractor SMS, store send attempts/receipts/template version beside email delivery metadata, ingest signed Twilio status callbacks when `TWILIO_AUTH_TOKEN` is configured, and preserve contractor-visible comments only for successful reviewed sends. Pending Remba review.
- [x] Contractor and maintenance invoice recovery polish v1: failed/skipped contractor email attempts now show recovery copy, retry attempt numbers, and a `Retry update` action without duplicating comments on untouched retries; maintenance-linked invoice provider exceptions show recovery guidance and a return path between the work order and Billing Readiness. Pending Remba review.
- [x] Maintenance completion handoff v1: dedicated work-order detail now has an Operations completion checklist for job completion, approval, contractor assignment/contact, evidence, invoice linkage, delivery readiness, and provider recovery, plus explicit Start job and Complete job actions that clarify what Billing Readiness inherits. Pending Remba review.
- [x] Contractor update templates v1: maintenance work-order detail now offers reusable attendance-window, quote follow-up, completion-evidence, and billing-document email templates that prefill the explicit provider send form while keeping dispatch operator-controlled. Pending Remba review.
- [x] Maintenance closeout notes/photos v1: work-order completion can now capture a final closeout note and optional closeout photo, store photo evidence through the document store, preserve existing operational notes, and keep the completion action explicit. Pending Remba review.
- [x] Contractor provider history v1: maintenance work-order detail now surfaces recent SendGrid contractor delivery attempts and receipts with status, attempt number, timestamp, and provider detail beside the explicit send/retry form. Pending Remba review.
- [x] Maintenance closeout history v1: each completion now stores a compact closeout history entry with status, timestamp, note, and closeout photo count, and the work-order completion panel surfaces recent closeout history for audit context. Pending Remba review.
- [x] Maintenance invoice recovery path v1: linked maintenance invoice cards now show a billing-owned recovery path for approval, delivery prep, provider retry, dispatch, and payment follow-up, while Billing Readiness adds work-order status, approval, contractor context, and retry guidance to maintenance-linked provider failures. Pending Remba review.
- [x] Maintenance closeout source links v1: closeout history entries now resolve their stored photo document IDs into source evidence links, so completion audit rows can open the exact uploaded closeout files. Pending Remba review.
- [x] Contractor template/version history v1: contractor email attempts now keep provider template key/version on send receipts and attempt history, and the maintenance Contractor card surfaces the current and historical template/version used. Pending Remba review.
- [x] Maintenance completion communication drafts v1: completing a work order now stores review-only owner, contractor, and tenant completion copy inside closeout metadata and surfaces it beside closeout history without sending anything automatically. Pending Remba review.
- [x] Maintenance reopen/edit and owner review v1: work-order detail now supports basic record corrections with expanded activity tracking, explicit reopen from completed/cancelled jobs with reopen history, and review-only owner completion receipt capture before any external message is sent. Pending Remba review.
- [x] Maintenance activity audit v1: work-order detail Activity now starts with a compact audit strip for latest update, external visibility, provider evidence, and closeout trail, and each timeline row carries an audience badge (tenant-visible, contractor-visible, provider evidence, internal/system audit) so operators can quickly tell what left Leasium versus what stayed internal.
- [x] Maintenance status forwarding drafts v1: work-order detail Activity now drafts tenant-to-contractor and contractor-to-tenant forwarding copy from the latest visible timeline row on each side. Operators can copy the draft locally with an explicit "No message sent" receipt; no email, SMS, portal message, provider update, or history mutation runs. Routing reviewed forwards through the Comms queue remains the broader automation follow-up.
- [x] Maintenance completion recipient review v1: closeout communication drafts now have explicit review paths for owner, tenant, and contractor copy. Each recipient gets a reviewed/not-reviewed state, review note, local review-only copy action, stored `communication_review` metadata, and a no-send guardrail so the operator can approve wording before any later email, portal, or contractor follow-up workflow.
- [x] Work assignment foundation v1: the Work queue and Maintenance/Arrears rows can assign maintenance jobs, arrears cases, and critical-date obligations to active platform members, store assignment history in metadata, and prepare an in-app notification receipt without sending provider email/SMS automatically. Pending Remba review.
- [x] Work assignment workload filters v1: the Work queue now shows team workload counts, unassigned/assigned totals, urgent load, and assignee filters for all open work, unassigned work, the current operator, or a selected member. Pending Remba review.
- [x] Work assignment reminder/escalation cues v1: new assignments now store an in-app reminder date and escalation watch date, the assignment control surfaces reminder/escalation badges with no-provider-send guardrails, and the Work queue can filter to assignment follow-ups due. Pending Remba review.
- [x] Work assignment action runners v1: assignment controls now let operators explicitly log in-app reminders and queue assignment escalations, updating assignment status/history without sending provider email/SMS. Pending Remba review.
- [x] Work assignment provider notice v1: assigned maintenance jobs, arrears cases, and critical-date obligations can now explicitly send or retry a SendGrid-backed operator email notice from the Work controls, with provider status, message ID, receipt history, and audit logs stored on the source record. Pending Remba review.
- [x] Work assignment provider receipts v1: SendGrid event webhooks can now update assignment notice status across maintenance, arrears, and critical-date work from queued into delivered/opened/failed receipt history, using assignment custom args or message-id lookup. Pending Remba review.
- [x] Work assignment notification preferences v1: operator records now store Work email preferences, Settings exposes a per-member Work email on/off control, and provider-backed assignment notices respect disabled operator preferences by recording a skipped receipt instead of sending. Pending Remba review.
- [x] Work assignment batch notices/history v1: the Work queue can send all ready assignment notices in the current view through the existing reviewed provider endpoints, and each assignment control now keeps receipt/history detail tucked into a compact recent-activity disclosure. Pending Remba review.
- [x] Work assignment notice inbox v1: the Work queue now has a compact notice inbox for the current view, grouping assignment notices into attention, ready, in-flight, and done states with recent receipt links back to the underlying work. Pending Remba review.
- [x] Work assignment digest preference v1: operator notification preferences now include a daily/weekly/off Work digest cadence beside the immediate SendGrid notice email toggle, ready for future scheduled summaries without another schema change. Pending Remba review.
- [x] Work assignment digest generation v1: `/api/v1/work-assignments/digests/run` generates review-only daily or weekly operator digest previews from currently assigned maintenance, arrears, and critical-date work, with Operations showing the generated digest and no-message-sent guardrails. Pending Remba review.
- [x] Work assignment digest receipts v1: generated Work digests now write compact per-operator preview receipts into notification preferences, preserving cadence, item counts, follow-up counts, and no-message-sent status for future cron/provider delivery history. Pending Remba review.
- [x] Work assignment digest receipt visibility v1: Settings now exposes each operator's latest digest preview receipt beside their Work notification preferences, while preference saves preserve the system-managed receipt history. Pending Remba review.
- [x] Work assignment digest scheduled runner v1: `/api/v1/work-assignments/digests/run-scheduled` can generate the same no-message-sent digest preview from a webhook-secret protected scheduler path, with system audit attribution for future cron wiring.
- [x] Work assignment notification center v1: `/notifications` and `/api/v1/work-assignments/notification-center` now centralise assignment notice states and digest preview receipts across maintenance, arrears, and critical dates, with a read-only guardrail and Work deep links. Pending Remba review.
- [x] Work assignment notification read state v1: operators can mark the notification center reviewed per entity, storing a read timestamp in notification preferences and showing unread counts against notice and digest activity. Pending Remba review.
- [x] Work assignment due-digest cron runner v1: `/api/v1/work-assignments/digests/run-due` lets a scheduler generate review-only daily, weekly, or all due digest previews across active entities without knowing each entity ID, using the shared webhook secret and skipping entities with no matching assigned work.
- [x] Dashboard and Work urgency hierarchy polish v1: overdue/status pills now carry the warning emphasis while Dashboard command rows, Needs attention, Events, and Work queue titles use calmer medium-weight typography with muted metadata. Pending Remba review.
- [x] Dashboard entity-title de-duplication v1: the live dashboard no longer repeats the selected entity name as a large content heading because the AppHeader entity selector already owns that context. Daily command center is now the first visible content heading and carries the compact demo/refresh controls in its header, with a screen-reader-only Dashboard H1 retained for page structure. Smoke coverage verifies the entity stays selected in the shell control and is absent as a content heading.
- [x] Work assignment digest email approval v1: manual and scheduled digest runs can send SendGrid-backed operator digest emails only when `send_email_approved` is explicit, store queued/skipped/failed delivery receipts in operator notification history, accept SendGrid digest webhooks, and surface digest delivery state in Operations, Settings, and Notifications. Pending Remba review.
- [x] Work assignment digest retry/recovery UI v1: Notifications digest receipts that have not sent now expose explicit `Send digest` or `Retry digest` recovery actions, using the approved digest-send endpoint and refreshing receipt state after delivery. Pending Remba review.
- [x] Work assignment digest recovery audit v1: digest delivery receipts now preserve delivery trigger, recovery source timestamp, provider attempt rows, and delivery attempt count so manual, scheduled, and recovery sends can be distinguished in Notifications and audit history. Pending Remba review.
- [x] Work notification preferences panel v1: Settings now has a dedicated Work notifications panel for per-operator assignment email toggles, SMS recovery phone controls, digest cadence, compact latest-digest receipts, preference status counts, and a collapsed advanced template-defaults disclosure, while the Users and roles table stays focused on access control. Pending Remba review.
- [x] Work notification history filters v1: Notifications now filters Work notices by attention, in-flight, ready, follow-up, and failed email state, and filters digest receipts by needs-send, sent, failed, skipped, and recovery state while keeping send/retry actions explicit. Pending Remba review.
- [x] Work notification channel and template evidence v1: notification-center responses now expose optional channel, provider, template key, and template version evidence for Work notices and digest receipts; Notifications adds channel filters for notice and digest history without changing send behavior. Pending Remba review.
- [x] Work notification template preferences v1: Settings lets owner/admin operators edit per-member assignment notice and digest template key/version defaults; provider sends and digest receipts use the reviewed operator defaults while preserving explicit send/retry approval. Pending Remba review.
- [x] Work notification template preview v1: Settings now translates per-operator Work notice/digest template keys into a compact SendGrid email preview with version badges, sample subject, and included content summary before saving. Pending Remba review.
- [x] Work notification provider history v1: Notifications now surfaces latest provider history rows for Work notices and digest receipts, including SendGrid status, event, timestamp, template/version, attempt count, and error detail where present. Pending Remba review.
- [x] Work notification recovery cues v1: Notifications now gives each Work notice and digest receipt a plain-English next-action cue for retry, wait-for-receipt, preference cleanup, or no-recovery-needed states while keeping provider sends explicit. Pending Remba review.
- [x] Work notification named template catalog v1: the API now exposes named SendGrid-backed Work notice and digest template records, and Settings lets operators choose named defaults with version badges and managed/custom previews instead of typing raw keys first. Pending Remba review.
- [x] Work notification direct email recovery v1: Notifications can now explicitly Send/Retry assignment notice emails from notice rows through a generic notification-center endpoint, preserving SendGrid/provider receipt history, operator email preferences, entity scoping, and idempotent already-sent guardrails. Pending Remba review.
- [x] Work notification channel readiness v1: notification-center responses now expose center-level Email/SMS/In-app readiness with actionable/blocked/read-only states, configuration evidence, and next-action guidance so non-email provider wiring has an honest migration-free surface. Pending Remba review.
- [x] Work SMS preference modelling v1: operator notification preferences now store a reviewed SMS enabled flag and phone number in the existing JSON preferences, Settings exposes the controls beside Work email/digest preferences, and Notifications uses those preferences to distinguish Twilio configuration gaps from missing operator phone data without sending SMS yet. Pending Remba review.
- [x] Work SMS send/retry wiring v1: Notifications can now explicitly Send/Retry assignment SMS through a Twilio-backed notification-center endpoint, storing SMS attempts under channel-specific notice metadata, preserving the existing SendGrid email summary, and accepting signed Twilio status callbacks without clobbering email receipts. Pending Remba review.
- [x] Work notification channel receipts v1: Work notice API responses now include additive per-channel receipt projections for Email and SMS, so Notifications can render channel evidence from one reusable shape while preserving legacy email and SMS fields during rollout. Pending Remba review.
- [x] Work notification receipt evidence disclosure v1: Notifications now keeps verbose per-channel provider evidence behind compact inline `Receipt evidence` disclosures, and email attempt counts stay tied to actual SendGrid send attempts after webhooks so delivery receipts do not look like retries. Pending Remba review.
- [x] Work notification rendered message previews v1: Work notice Email/SMS receipts and Work digest receipts now carry read-only rendered message previews generated from the same delivery templates, with Notifications showing collapsed `Message preview` disclosures before any provider send/retry action. Pending Remba review.
- [x] Work notification provider setup checks v1: notification-center channel readiness now includes collapsed provider-console checks for Work email, SendGrid sender/webhook, operator SMS preferences, Twilio messaging, and Twilio callback setup, exposing only bare webhook endpoints and no secrets or tokenized URLs. Notifications also has a local `work-notification-provider-readiness.csv` export for channel readiness, setup-check status, next actions, and guardrails from the already-loaded notification-center response; it does not send email/SMS, run digests, mark notifications read, refresh provider tokens, dispatch providers, or mutate provider history. Pending Remba review.
- [x] Ownership tags directory v1: Settings Organisation now aggregates the same owner/billing identity chips shown on property rows into a read-only tag directory with source context, property counts, property links, tag-level links into a filtered Properties view, and clickable owner chips on property rows. Pending Remba review.
- [x] Property image helper experiment v1 (closed as pulled): shipped 2026-05-22 and pulled the same day. The OpenAI web-search helper returned listing page URLs as image candidates without verified direct image file URLs, so candidate cards rendered without thumbnails and the panel felt broken in production. Per the original "pull it if visual quality or source clarity is poor" guidance, the v1 OpenAI search source was retired; the helper surface was later rebuilt as v2 below with SerpAPI direct-image candidates, the SSRF-guarded stored-document workflow, Pillow processing, and refreshed smoke/backend coverage.
- [x] Property image helper experiment v2 (SerpAPI Google Images): the property image preview/apply routes, the Portfolio property image panel and row thumbnails, the Pillow processing pipeline, and the SSRF-guarded stored-document workflow are back. Search now goes through `stewart/integrations/serpapi_image_search.py`, which calls SerpAPI's Google Images endpoint with the property address (`google_domain=google.com.au`, `gl=au`) and returns verified direct HTTPS image URLs plus the source page and domain. Confidence is derived from result position. The preview response renames `openai_response_id` to `provider_response_id` and the route returns a 503 with a clear message when `SERPAPI_API_KEY` is unset (no record mutation). Pending Remba review.
- [x] Portfolio QA guided cleanup v1: `/portfolio-qa` now turns cleanup into guided actions for tenant contact fixes, owner/billing identity fixes, per-row or batch tenant onboarding link creation, and richer source trails. Source history now expands register import workbook rows, Smart Intake apply history, field citations, and tenant public-enrichment apply history through the reusable evidence trail pattern. Pending Remba review.
- [x] Portfolio QA completion report + bulk staging v1: `/portfolio-qa` now opens with a completion report across data QA, tenant contacts, owner billing identity, onboarding prep, billing drafts, and source trails. It also surfaces AI-assisted enrichment candidates for properties and tenants with missing public-safe fields, adds a compact blocked-followups queue for the next cleanup rows, shows a row-level blocker drilldown for active bulk groups, and adds reviewed bulk staging for obvious tenant-contact and owner-billing fixes before the operator saves staged rows. The downloadable cleanup CSV includes matching blocker-drilldown rows so the SKJ handoff can be reviewed without tab-hopping, and the enrichment queue has its own local CSV export for candidate type, record label, missing fields, priority, action, and review guardrail. The Onboarding prep and Billing drafts tabs now include blocker-review summaries that group ready invites, contact/recovery/setup blockers, owner billing fixes, rent-roll billing blockers, and existing internal drafts before the operator runs a batch. The report now carries a final-readiness verdict (`Register cleanup still blocked`, `Ready after enrichment review`, or `Ready for SKJ tuning`) so the next step is explicit. No automatic enrichment or provider action runs. Live SKJ tuning remains a follow-up.
- [x] Communication template registry and invoice preview evidence v1: Settings Organisation now shows shared communication template keys, versions, preview copy, delivery guardrails, and provider receipt endpoints across invoice delivery, tenant onboarding, Work notices/digests, contractor email/SMS, and tenant portal preferences. Invoice delivery prep now stores the configured `invoice_delivery` template key/version plus a normalized rendered message preview in delivery metadata, and Billing Readiness shows the version badge and collapsed email preview. Pending Remba review.
- [x] Work digest channel receipts v1: Work digest receipt rows now expose the same additive `channel_receipts` projection used by Work notices, so Notifications can render a normalized email Receipt evidence disclosure alongside the existing top-level digest summary and rendered message preview. Pending Remba review.
- [x] Maintenance contractor channel receipts v1: `MaintenanceWorkOrderRead` now exposes a normalized `channel_receipts` projection for the contractor email and SMS paths, computed from the existing `contractor_delivery` metadata so no schema migration is needed. The work-order detail page now shows a compact `Channel evidence` disclosure beside the existing inline contractor controls, surfacing recipient, template/version, attempt count, last event, timestamps, provider history, and a collapsed message preview per channel. Pending Remba review.
- [x] Xero configurable reconciliation stale window v1: the previously-hardcoded 7-day payment reconciliation stale window is now sourced from `XERO_RECONCILIATION_STALE_AFTER_DAYS` (defaults to 7). `/xero/status` returns the configured value in `accounting_freshness.stale_after_days`, so widening or tightening the window propagates to Settings, Billing Readiness, Insights, and public snapshot accounting-readiness blocks without code changes. Fresh states now use plain `Reconciliation current` copy while stale states still show the configured window. Pending Remba review.
- [x] Contractor SMS templates v1: the maintenance work-order page now offers reusable attendance-window, status-update, completion-check, and billing-documents SMS templates that pre-fill the contractor SMS textarea, mirroring the existing contractor email template selector. Operator-edited text resets the selector back to Custom; successful sends reset both the template selector and the body. Provider dispatch stays operator-controlled. Pending Remba review.
- [x] Settings integrations health card v1: new `/api/v1/system/integration-status` returns boolean configured/not status (plus label, purpose, detail copy, and non-secret webhook URLs where relevant) for SerpAPI, OpenAI, SendGrid, Twilio, Xero, and DocuSign without leaking any keys, secrets, or account IDs. Settings Organisation tab renders the data as a compact "Integrations" card so operators can see at a glance which providers are wired up on the API service; DocuSign additionally calls out missing `DOCUSIGN_WEBHOOK_SECRET` before live Connect testing and shows the Connect webhook URL when `PUBLIC_API_URL` is set. Pending Remba review.
- [x] Branded communication templates foundation v1: new `branded_communication_template` table (migration `20260522_0022`), SQLAlchemy model, Pydantic schemas, and read-only API at `/api/v1/branded-communication-templates`. Each row is an editable per-entity override for an in-code template key (e.g., `work_assignment_notification`, `invoice_delivery`, `maintenance_contractor_update`) with subject/body/action-label/action-url-template, an `is_system` flag, and full audit fields. Settings Organisation now shows the stored override rows in a read-only review panel beside the runtime communication registry, including active override coverage against runtime template keys. No actual sends consume the table yet — Settings editor UI + actual send wiring are paused while SKJ uses Leasium internally. Pending Remba review.
- [x] Dashboard upcoming lease events v1: the operator dashboard now opens with a compact "Upcoming lease events" panel summarising the next 120 days of lease expiries, rent reviews, obligations, and onboarding follow-ups using the existing `/insights/overview` data (`lease_event_snapshot.next_events`). Each row shows the event kind badge, title, and chip-formatted date and deep-links into the relevant property/lease. Counts in the panel header summarise expiries/reviews/overdue/due-soon at a glance. Pending Remba review.
- [x] MVP dashboard performance pass (2026-05-30): the dashboard now has a first-paint `/api/v1/dashboard/overview` endpoint, starts from the last selected entity before `/entities` settles, renders command-center and metric data progressively while heavier detail queries finish, shares a root React Query cache, warms/dedupes Clerk auth tokens, disables persistent shell link prefetch, and adds a repeatable `npm run audit:live` desktop/mobile audit harness. Backend support now includes Clerk JWKS client reuse, request timing headers/logs, configurable DB pooling, and Vercel same-origin `/api/v1` rewrites. Details: [`docs/mvp-ux-performance-review-2026-05-30.md`](mvp-ux-performance-review-2026-05-30.md).
- [x] Property next-expiry chip v1: each property row in the Properties table now also shows the earliest upcoming active-lease expiry within 120 days as a colour-coded chip beside the occupancy badge (red <30 days, amber 30-60, neutral 60-120). Derived from the existing rent-roll data; no backend change. Vacant or holding-only properties show no chip. Pending Remba review.
- [x] Property occupancy badge v1: the Properties table now shows a status badge inline next to each property name (Leased / Partial / Vacant / No units) derived from the existing rent-roll rows; active and holding-over leases count as occupied. A pill-row filter above the table lets operators show only properties in a chosen state, with counts per state visible at all times. Pending Remba review.
- [x] Property occupancy: Leased internal v1: the occupancy badge now distinguishes `Leased internal` (purple) from `Leased` (green). Internal is auto-derived by matching the rent-roll tenant name against the property's `owner_legal_name`, `trustee_name`, `trust_name`, or `invoice_issuer_name` (case-insensitive, trimmed). A property qualifies for `Leased internal` only when every unit is leased AND every lease is internal; mixed cases stay `Leased` (with an "N internal" suffix). Filter pills gain a `Leased internal` option. No explicit per-lease override yet — that's a follow-up if the auto-derive misses cases in production. Pending Remba review.
- [x] Mobile-polish v2 — AI Inbox surface touch targets. The "Take it from here" Link on the classification card (the primary action that hands the operator off to the suggested Leasium surface) moved from `px-3 py-1.5 text-xs` (≈32px tall) to `min-h-11 px-3 text-sm` (44px). The rest of the surface (Try sample / Reset / Classify buttons, textarea) was already on Button/SecondaryButton baselines that meet HIG. Smoke coverage now runs the classify-and-deep-link path at 390px width and measures the handoff link as a 44px touch target. Pending Remba review.
- [x] Mobile-polish v2 — Billing Readiness surface touch targets. Primary action links (Open recovery, Review payments, Open statements, Preview, PDF, foundation links) and Dispatch & reconcile delivery filter buttons (All / Needs action / Ready to dispatch / Complete / Unpaid) now use 44px touch targets, replacing the earlier 40px filter-chip compromise. Month-end handoff buttons also sit on the 44px baseline. Smoke coverage now runs Billing Readiness at 390px width, measures the filter chips, clicks only those read-only filters under a no-mutation request guard, and measures checklist/invoice-row handoffs without clicking dispatch, email, payment, provider, or reconciliation actions. Pending Remba review.
- [x] Mobile-polish v2 — Operations surface touch targets. On `/operations`: three primary action Links (Open tenants, Review, Review completion) use `min-h-11`; workload filter pill chips (Open / Unassigned / Follow-up due / My work / per-member) now use the 44px target baseline, with companion Team workload / Assigned chips lifted for alignment. On `/operations/maintenance/[workOrderId]`: the Operations back link, invoice-handoff Links (Open in Billing, Recover in Billing, Preview, PDF), and documents Download anchor use 44px targets. SecondaryButton overrides with `min-h-9` left alone — verified dead code (component baseline is `min-h-11`). Three-tab operations strip remains `min-h-16`. Smoke coverage now runs both `/operations` and the maintenance detail at 390px width and measures the tabs, workload chips, primary links, and invoice handoffs. Pending Remba review.
- [x] Mobile-polish v2 — Operations row density follow-up. Below `xl`, assignable queue rows now collapse assignment + row actions into a closed `Work controls` disclosure, and maintenance rows collapse assignment + completion/detail actions into `Work-order actions`. The closed summaries now include assignee state plus work context (queue chip, maintenance priority/status, and notice state when present) so phone operators can triage without opening every row. Desktop keeps the existing always-visible columns. Smoke coverage verifies the compact 390px viewport path, including hidden-until-open controls and visible completion handoff after expansion. Pending Remba/live-phone review.
- [x] Mobile-polish v2 — Notifications surface touch targets. Work notice and Digest history filter buttons now use the full 44px target baseline (`min-h-11`), replacing the earlier 40px filter-chip compromise; the per-row "Open work" Link and bottom-of-page "Open Work" Link also sit at 44px. The per-row SecondaryButtons with `h-9 px-2.5` overrides were verified as dead code — `SecondaryButton` already has `min-h-11` baseline and CSS `min-height` wins over `height`, so those buttons were already 44px. Smoke coverage now runs Notifications at 390px width and measures every status/channel filter in both panels plus Open work/Open Work links without touching send/retry/mark-reviewed actions. Pending Remba review.
- [x] Mobile-polish v2 — Tenant detail onboarding row collapses provider detail on mobile. Below `md`, the Delivery / Reminders / Expires / Applied provider sub-grid plus delivery copy, reminder schedule, and optional delivery timeline now sit behind a closed `<details>` "Provider detail" disclosure with a 44px summary target. Status badge, due date, action button cluster, and Submitted-for-review review stay always visible. Desktop keeps provider detail inline. Smoke coverage verifies mobile collapse/expand and desktop inline rendering. Pending Remba review.
- [x] Mobile-polish v2 — Settings tab buttons bumped from `min-h-10` to `min-h-11` to meet 44px Apple HIG. Audit's horizontal-scroll concern not applied — Settings only has three tabs and already uses `flex-wrap`. Smoke coverage now runs Settings at 390px width and measures Security, Organisation, and Xero tabs as 44px touch targets. Pending Remba review.
- [x] Mobile-polish v2 — `AppHeader` icon-only buttons (hamburger, mobile drawer close, search, keyboard-cheatsheet, notifications, appearance) bumped to `h-11 w-11` (44px) for Apple HIG. Clerk operator-user-control wrapper also moves to `h-11` so the cluster sits on a consistent baseline. Icon visuals unchanged. Smoke coverage measures the rendered mobile navigation open/close/search/notification/appearance targets and confirms the keyboard-cheatsheet button stays hidden below `sm`. Pending Remba review.
- [x] Mobile-polish v2 — Tenants list mobile card view: below `md`, the Tenants `Onboarding command center` table is visually hidden and a stacked card list takes over. Each card shows tenant name, the onboarding status badge, contact email/phone fallback, due-date chip (red when overdue), and a contact-needs-fix warning when present. Tapping the card opens the existing `TenantQuickViewDrawer`, which links to the full record where the action cluster (Copy link, Resend, Cancel, Invite to portal) lives. Desktop table is unchanged. Smoke coverage verifies the 390px card path and drawer handoff. Driven by `docs/mobile-ux-review-2026-05-23.md` priority 1. Pending Remba review.
- [~] Tenant onboarding entrypoint v2 — Send invite from the Tenants page. The primary CTA at the top of `/tenants` is now "+ Send invite" (replaces "+ Add tenant"). Form collapses to five required-ish inputs: Property dropdown, Unit dropdown (filtered by property), Tenant name (saved as `legal_name`), Contact email (where the invite goes), plus optional Contact name and Onboarding due-date. Submit fires four sequential calls in the frontend: `createTenant` → `createLease` (pending status, all dates null) → `createTenantOnboarding` → `sendTenantOnboardingPortalInvite`. The whole agent journey collapses from "create tenant → go elsewhere → create lease → go elsewhere → create onboarding → click invite" to one click. The tenant fills the remaining tenant-record fields (ABN, billing email, phone, insurance, emergency contact, notes) themselves via the portal — gated by Clerk sign-up, so submitted data is bound to an authenticated identity rather than a token-in-email. Backend data model already supported this; no schema/migration. Pending Remba review.
- [~] Tenant portal onboarding v1: account-only onboarding through the tenant portal. Operator's primary action on a live onboarding row is now a "Invite to portal" button (new `POST /tenant-onboarding/{id}/send-portal-invite`) that fires a dedicated `tenant_portal_invite` SendGrid/Twilio template pointed at `/tenant-portal/[token]`. Tenant signs in via Clerk, lands on the existing portal dashboard, and a new "Complete your onboarding" panel collects legal name, ABN, contact, billing, insurance, emergency contact, and notes — prefilled from `submitted_data` if they've been here before, or from the tenant record. Saves go through a new authenticated `POST /tenant-portal/onboarding/submit` that writes to `submitted_data` only; the existing operator review/apply remains the only path that mutates the canonical tenant record. After submission the panel collapses to a read-only "Your property manager will review and confirm" state. The standalone `/onboarding/[token]` public form is retired — old links now show a redirect screen pointing to the portal. Provider sends still require operator approval (no auto-trigger on Apply). Pending Remba review of the operator CTA placement vs the existing Copy link / Resend cluster, the portal panel copy and density, and whether the retired-form screen needs a Remba-styled illustration.
- [~] Tenant onboarding room v1 (2026-05-24). First-run tenant experience is now a focused onboarding room, not the full portal. After account setup, tenants whose onboarding is not yet `applied`, or whose onboarding is applied but lease signing is still incomplete, see one job: get the tenancy ready. The room shows a first-viewport summary (tenant, property/unit, due date, account-ready badge), a checklist (account, confirm details, required documents, property-team review, lease pack/signing), the existing review-first onboarding form/status, onboarding-only document upload/list, a lease snapshot, and "what happens next" copy. Payments, maintenance, notification preferences, invoice metrics, and general portal guardrail/debug panels are hidden until lease signing is complete. The legacy `/onboarding/[token]` public page no longer fetches token-scoped portal data; it simply hands tenants to the account-first portal gate. Smoke mocks now include the invite preview endpoint and assert the token invite is account-first. Pending Remba review of checklist wording, whether the form should split into Identity / Insurance / Emergency sections, and the exact unlock state for lease signing vs. full portal.
- [~] Tenant onboarding room v1.1 (2026-05-25). Account-first stays the product rule. Tenant confirm-details is now focused on legal name, contact name, contact email, and contact phone, with trading name, ABN, billing email, insurance expiry, emergency contacts, and notes tucked into an optional disclosure. Tenant-facing `reviewed` state is softened to "In review" / lease-pack handoff copy so tenants do not see internal review/apply mechanics. The required-documents step now shows "Not required" and counts as complete when no document checklist exists. Operator tenant detail now collapses the routine submitted → reviewed → applied path into one primary action: "Approve & apply" when no lease-signing blockers exist, "Approve for signing" when signing must happen next, and "Mark reviewed" when lease questions are still blocking. Pending Remba review of the simplified tenant form and action labels.
- [~] Operator tenant portal preview v1 (2026-05-24). The old "Preview as tenant" button no longer opens the tenant's token URL. Tenant detail now links to a read-only operator preview at `/tenants/{tenantId}/portal-preview/{onboardingId}` backed by `GET /api/v1/tenant-portal/operator-preview/{onboarding_id}`. The endpoint reuses the tenant-visible portal payload through the signed-in operator session (`auth.mode = operator_preview`) and the UI shows lease, checklist, documents, payments, maintenance, contact, and guardrails without tenant mutation actions. The preview now mirrors tenant-friendly onboarding language for `reviewed`, shows a "Not required" checklist row when no documents are requested, and can download a local `tenant-portal-preview-{tenant}.csv` packet covering tenant, lease, onboarding, checklist, document names/counts, visible invoices/payment rows, maintenance rows, contact-change requests, and preview guardrails. The CSV does not create portal accounts, send invites, submit tenant details, apply/dismiss contact changes, send email/SMS, upload/delete documents, fetch document bytes, write Xero data, dispatch providers, refresh providers, or mutate provider history. Pending Remba review of whether preview access should be audited and whether document download should remain available from the preview.
- [x] Tenant portal maintenance status clarity v1 (2026-05-25). Tenant maintenance request cards now include a plain-language status line for requested, triaged, assigned, awaiting approval, approved, in progress, completed, and cancelled states, using due/completed dates where available. The operator preview mirrors the same wording so the team sees what tenants see, and focused browser smoke now exercises the full status matrix without creating portal accounts, sending messages, dispatching providers, or mutating tenant/provider state. The broader tenant-side activity feed is now closed separately.
- [x] Tenant portal compliance empty-state polish (2026-05-25). The tenant portal Compliance panel and read-only operator preview now handle tenancies with an explicit empty `portal_compliance_checklist`: the API returns no required checklist rows while still exposing accepted upload categories and uploaded supporting files, the UI shows a "Not required" badge plus an explicit empty row instead of a blank checklist area, and the preview CSV includes the no-required-documents guardrail. Focused backend coverage proves token and operator-preview responses return an empty checklist when metadata opts out, and browser smoke proves the operator preview renders the empty checklist row without tenant mutation actions.
- [~] External design review 2026-05-23 — nav trim 13 → 8. Primary sidebar `navItems` in `apps/web/src/components/app-shell.tsx` reduced from thirteen items (Dashboard, Smart Intake, AI Inbox, Properties, Tenants, Work, Contractors, Comms, Billing, Insights, Statements, Portfolio QA, Settings) to eight (Dashboard, Smart Intake, Properties, Tenants, Work, Billing, Insights, Settings). Removed items stay reachable from the Cmd-K command palette via explicit `commandActions` entries and from the keyboard `G`-shortcuts via `SHORTCUT_NAV`, so operators with muscle memory keep working. Unused `HardHat / HelpCircle / Inbox / Receipt / Send` icon imports also cleaned up. The Properties + Tenants → Portfolio consolidation toward the 7-item target in the 2026-05-21 nav simplification note is deferred until a Portfolio landing exists. Driven by `docs/external-design-review-2026-05-23.md` §1.1. Pending Remba review.
- [~] External design review 2026-05-23 — typography hierarchy restore. `apps/web/src/components/ui.tsx` now exports a `PageTitle` (`<h1>`, 30/36/600, tracking-tight) and `SectionTitle` (`<h2>`, 18/28/600, tracking-tight). `PageHeader` uses `PageTitle` instead of an inline `<h2 className="text-xl">` (20px → 30px), and `SectionPanel` uses `SectionTitle` instead of an inline `<h3 className="text-[15px]">` (15px → 18px). This also corrects the document outline: workspace pages are now `<h1>` (PageTitle) → `<h2>` (SectionTitle) rather than starting at `<h2>` → `<h3>`. The SoT scale (H1 36/44, H3 24/32) was scaled down to operator-mode values (30/36 + 18/28) to avoid marketing weight on dense pages like Properties/Operations/Billing Readiness. Smoke tests use level-agnostic `getByRole("heading", ...)` so no test churn. Driven by `docs/external-design-review-2026-05-23.md` §1.3. Pending Remba review.
- [~] External design review 2026-05-23 — Dashboard reshape. Metric grid in `apps/web/src/components/dashboard.tsx` cut from six cards (`xl:grid-cols-6`) to four operational cards (`lg:grid-cols-4`): Operations, Billing blockers, Needs review, Blocked docs. Properties and Tenants metric cards removed — they were navigational counts, not "act now" metrics, and the sidebar already links to both surfaces. Two-column Smart Intake + Needs attention section hoisted from position 8 (below Activity feed) to position 2 (right after Daily command center), so the drop zone is in the first viewport at common laptop sizes. The same two-column section is reused in `/intake` workspace mode, so intake users benefit from the same lift. Orphaned helpers (`propertiesLoading`, `tenantsLoading`, `displayPropertiesCount`, `displayTenantsCount`, `portfolioOccupancy`, `propertiesOccupancySummary`) and now-unused `portfolioOccupancyTotals` / `propertyOccupancyFromRentRoll` imports cleaned up. Driven by `docs/external-design-review-2026-05-23.md` §3.1 + §3.2. Pending Remba review.
- [~] Reduced-motion global override — accessibility win from external review §9. `apps/web/src/app/globals.css` adds a `@media (prefers-reduced-motion: reduce)` rule that pins every transition/animation to ~0.01ms when the user has "Reduce Motion" set at the OS level. Information still conveys (buttons change colour, panels still appear/disappear) but without the visual delay. SoT §10.5.7a documents the rule + the `motion-safe:` opt-out path for any animation that genuinely conveys state. Pending Remba review.
- [~] Dashboard order — metric strip hoisted to position 2 (operator override 2026-05-23). The four-up metric strip in `apps/web/src/components/dashboard.tsx` (Operations, Billing blockers, Needs review, Blocked docs) moved from position 3 to position 2 — now: Command Center → Metric strip → Smart Intake/Needs Attention → Lease events → Ask Leasium → Activity feed. Per direct operator request: the cards carry status chips + trend deltas + per-card next-action copy, so the at-a-glance scan lands better adjacent to the Command Center than below Smart Intake. SoT §10.5.6 updated with the override + rationale so the rule doesn't drift back. Pending Remba review.
- [~] External design review 2026-05-23 — Page-file split phase 3 (dashboard.tsx). Extracts `DashboardCommandCenter` component + its `CommandCenterItem` and `CommandCenterCounts` types into `src/components/dashboard/DashboardCommandCenter.tsx` (214 lines). Parent imports both types + the component; `commandCenterSort` stays in dashboard.tsx because it leans on the parent-local `dueRank` date helper. Net: dashboard.tsx 3,751 → 3,577 lines (−174). Cumulative phases 1+2+3: 4,463 → 3,577 (−886, 20% reduction). Phase 4+ queue: DocumentIntakeApplyOutcomeCard (248 lines), DocumentIntakeReviewPanel (646 lines, biggest), plus general-purpose helpers (dueRank/dateOnly/dueLabel/formatDate/formatMoney/intakeStatusTone/obligationTone) into `src/lib/`. Driven by `docs/external-design-review-2026-05-23.md` §1.2 + SoT §10.5.3. Pending Remba review.
- [~] External design review 2026-05-23 — Page-file split phase 2 (dashboard.tsx). Extracts `DashboardMetricCard` family (the metric tile + `DashboardMetricTrend` type + `MetricSparkline` + `MetricDeltaBadge` sub-helpers + `computeOpenObligationTrend` 7-day roll-up) into `src/components/dashboard/DashboardMetricCard.tsx` (217 lines), and `UpcomingLeaseEventsPanel` family (the panel + `leaseEventKindLabel` + `leaseEventKindTone`) into `src/components/dashboard/UpcomingLeaseEventsPanel.tsx` (138 lines). Parent imports both. Orphaned `LeaseEventRecord` type import cleaned up. Net: dashboard.tsx 4,048 → 3,751 lines (−297). Cumulative phases 1+2: 4,463 → 3,751 (−712, 16% reduction). Phase 3+ queue: DashboardCommandCenter (159 lines + types), DocumentIntakeApplyOutcomeCard (248 lines), DocumentIntakeReviewPanel (646 lines), plus general-purpose helpers (formatDate / formatMoney / intakeStatusTone / obligationTone / dueRank) into `src/lib/`. Driven by `docs/external-design-review-2026-05-23.md` §1.2 + SoT §10.5.3. Pending Remba review.
- [~] External design review 2026-05-23 — Page-file split phase 1 (dashboard.tsx). Establishes `apps/web/src/components/dashboard/` directory pattern and extracts the two cleanly-bounded panels from the 4,500-line dashboard.tsx: `AskLeasiumPanel` (227 lines incl. citation-kind helper + suggestion chips) and `ActivityFeedPanel` (180 lines incl. action-kind tone map, formatRelativeTime, activityTimeBucket helpers). dashboard.tsx imports both from new files; behaviour preserved. dashboard.tsx 4,463 → 4,048 lines (−415). Orphaned `askLeasium` / `AskCitationRecord` / `AskRecord` / `ActivityActionKind` / `ActivityFeedItemRecord` / `listActivityFeed` / `Activity` icon imports cleaned up. Same-commit consolidations: `friendlyError(error)` moved to `apps/web/src/lib/utils.ts` (was duplicated inline in 15 pages); `StatusTone` union exported from `apps/web/src/components/ui.tsx` (was duplicated in 7 pages). Existing inline copies stay for now; new extractions import from the canonical sources. Pattern ready for phase 2+ extractions (DashboardCommandCenter, UpcomingLeaseEventsPanel, DocumentIntakeApplyOutcomeCard, DocumentIntakeReviewPanel, DashboardMetricCard ~1,500 more lines) and for the three other monolithic page files (property-workspace.tsx, settings/page.tsx, operations/page.tsx). Driven by `docs/external-design-review-2026-05-23.md` §1.2 + SoT §10.5.3. Pending Remba review.
- [~] External design review 2026-05-23 — Mobile/tablet IA: sidebar collapses at md instead of disappearing at lg. `apps/web/src/components/app-shell.tsx` aside class changed from `hidden lg:flex lg:w-60` to `hidden md:flex md:w-16 lg:w-60` — three states: hidden + hamburger drawer below md, 64px icon-only rail at md (768-1023px), full 240px at lg+. Brand text, nav labels, and shortcuts text hide at md via `md:hidden lg:inline`/`lg:block`; the Work nav keeps a compact comms-count badge visible when drafts are queued. `title` attributes on each nav link surface labels on hover. Active state at md drops the 2px left rail (clashes with 64px-wide square rows) but keeps the `bg-white/[0.12]` surface tone. `apps/web/src/app/globals.css` adjusts `body.app-shell-active` padding-left across breakpoints (0/64/240px). Hamburger button class changed from `lg:hidden` to `md:hidden` so it only shows below md. SoT §10.5.4a documents the three-state pattern. Bottom-nav at sub-md (review §8.2) deliberately deferred per CLAUDE.md §2.3 internal-first scope. Driven by `docs/external-design-review-2026-05-23.md` §8.1. Pending Remba review.
- [~] External design review 2026-05-23 — Container hierarchy: Surface archetype. `apps/web/src/components/ui.tsx` now exports a `<Surface>` component as a sibling to `<SectionPanel>` — no card chrome, just a heading + optional description/actions + divider rule. For workspace-body content (tables, lists, dense data on a continuous canvas) where SectionPanel's card chrome was producing the "stack of look-alike white cards" pattern flagged in review §4. Same-commit typography drift cleanup: three small pages (`/contractors`, `/comms`, `/statements`) had raw `<h1 className="text-2xl font-semibold">` page titles surviving the typography ladder fix; replaced with `<PageHeader>` so they render at the canonical 30px. Surface adoption deferred into the page-file split queue rather than retrofitting the four monolithic workspace pages now. SoT §10.5.4 updated with the two-archetype container rules. Driven by `docs/external-design-review-2026-05-23.md` §4. Pending Remba review.
- [~] External design review 2026-05-23 — Loading-state polish v1. New `SkeletonLine` + `SkeletonRows` primitives in `apps/web/src/components/ui.tsx` (pulse-animated rows with `divide-y` separators, `aria-busy="true"` + `aria-label="Loading"`). Replaces `<EmptyState title="Loading X." />` thin-centered-text loaders in Dashboard (4 spots), `/contractors`, `/comms`, `/statements`, `/tenants` (table-cell + mobile-card), and `/tenants/[tenantId]` (full-page). 2026-05-31 follow-up: `/operations/maintenance/[workOrderId]` now uses the same skeleton primitive for the initial work-order wait and Correspondence panel wait instead of raw spinner-plus-copy rows, with mobile smoke coverage delaying both reads. SoT operational guardrail §10.5.5a documents the canonical loading pattern (SkeletonRows for list/table waits; never EmptyState; never bare "..." strings). Driven by `docs/external-design-review-2026-05-23.md` §1.5. Pending Remba review.
- [~] External design review 2026-05-23 — Active-nav surface tone + chip token cleanup. Two bundled fixes in `apps/web/src/components/app-shell.tsx` and `apps/web/tailwind.config.ts` + 12 consumer files. (1) Sidebar active-nav state was `bg-leasium-blue-soft/10` (perceptually invisible on navy-900); now `bg-white/[0.12]` for a real selected-surface tone, hover bumped to `bg-white/[0.06]`. (2) Status chip text colors were hex literals (`text-[#027A48]`, `text-[#B54708]`, `text-[#B42318]`) in 40 places across 12 files; tailwind config now exports named tokens `leasium-success-strong`, `leasium-warning-strong`, `leasium-danger-strong` and all literals bulk-replaced. Side benefit: several `border-leasium-*-strong/30` references in property-occupancy.ts and tenants/page.tsx that were silently dead (tokens didn't exist) now render as intended. Driven by `docs/external-design-review-2026-05-23.md` §2.1 + §5. Pending Remba review.
- [~] External design review 2026-05-23 — Public onboarding first-impression. `apps/web/src/app/onboarding/[token]/page.tsx` rewritten from a 60-line "Your onboarding has moved into your Leasium account" redirect stub into a real first-impression surface. Loads tenant + lease + onboarding context from the existing `getTenantPortal(token)` public endpoint (no new API) and renders welcome-by-name, property/unit chip with address, due-date + link-expiry chip, status pill, prominent "Continue to portal" CTA, and a "Nothing is applied until your property manager reviews and confirms" trust line. Three render states: skeleton while the token resolves, full welcome card on success, and a calm fallback when the token is missing/expired/errors. Separate "already submitted" path swaps the CTA to "Open your portal" and drops the action chips. Page remains public (Clerk-guard smoke still passes); the Tenant portal onboarding v1 retire-the-form decision is preserved — this is a calmer redirect surface, not a form restoration. Driven by `docs/external-design-review-2026-05-23.md` §1.4. Pending Remba review.

- [x] Owner statement review-first dispatch: `POST /api/v1/owners/statements/send` sends one reviewed owner statement via SendGrid only after explicit per-owner approval (`approve=true`), gated by `owner_statement_email_enabled` (off by default), recipient readiness, and idempotency per owner+month. Receipts persist in a new `owner_statement_dispatch` table (migration `20260530_0027`) and read back via `GET /api/v1/owners/statements/dispatch`. `/statements` adds a two-step-confirm Send/Resend action with an inline receipt. No Xero posting, payment reconciliation, invoice dispatch, or tenant email side effects.
- [x] Comms badge-counts fast path: `/comms/queue/counts` now runs the eight queue scanners in a `summary_only` mode that skips the per-row Property/Unit/Lease lookups and body-string building (the N+1 that made cold loads 6-8s), guarded by a parity test asserting counts equal the full `/queue` grouping; `/queue` behaviour is unchanged.
- [x] Portfolio QA blocked-row bulk review depth: per-blocker-reason breakdown with uncapped counts, plain-English "why it matters" copy, and guided review-first fix links. Read-only; bulk draft creation still routes through the existing approval.
- [x] Smart Intake inbound-attachment cues: promoted inbound-email attachments now stamp `inbound_sender`/`inbound_received_at`, and the review row shows From / Subject / Received.
- [x] World-class UX audit Phase B-E close-out: date-bucket headers on upcoming events + command center (B3), redundant billing-link icon removed (C4), Properties compact-density toggle (C6), Insights guardrail label split (C8), Operations complete/waive confirmation toast (E6), and owner-tag chain labels with cleaner chevrons (C2) while preserving raw filter keys/titles. Sidebar restructure (C7) remains deferred per the audit.
- [~] Dashboard list-row motion token normalization: command-center, Upcoming lease events, and activity-feed rows now share the 200ms `ease-leasium` transition timing for hover/focus polish. Secondary Upcoming/Activity rows also share the reduced-motion-safe row-enter animation, with source smoke coverage guarding drift. Pending prototype review on real dashboard data.
- [~] Dashboard event urgency differentiation: upcoming lease-event rows now turn near-term chips into `Due today`, `Due tomorrow`, or `Due in Nd`, so repeated same-day labels remain clear when rent reviews, expiries, obligations, and onboarding items share a bucket. Pending prototype review on real dashboard data.
- [~] Dashboard progressive disclosure: long Upcoming lease event lists now open with the first five rows and explicit `Show all` / `Show fewer` controls, matching the Recent activity audit feed's collapsed-first pattern and keeping the command-center viewport scannable. Recent activity's disclosure control now also sits on the 44px touch-target baseline. Pending prototype review on real dashboard data.
- [~] Dashboard Leasium AI touch-target polish: read-only suggestion chips and citation source links now use the 44px target baseline and shared motion timing while preserving the same ask flow, citations, and no-mutation guardrail. Pending prototype review on real dashboard data.
- [~] Saved views touch-target polish: the saved-view trigger, save input/button, Apply, Rename, Delete, and Close controls now use the 44px baseline with shared motion timing, and smoke coverage clicks the first-row actions so the close control cannot drift back over Delete. LocalStorage saved views and URL-filter re-apply behaviour are unchanged. Pending prototype review on real tenant/property/operations filter use.

## Next Build Order

The 2026-05-22 UX review (`docs/ux-review-2026-05-22.md`) reframes the
short-term backlog into three tiers benchmarked against Linear, Stripe,
Notion, Vercel, Re-Leased Credia, and AppFolio. **Tier 1 is active**;
Tier 2 follows after the highest-leverage Tier 1 items land.

### DoorLoop benchmark refocus (2026-05-31) — current top priority

Benchmark + gap analysis: [`doorloop-benchmark-2026-05-31.md`](doorloop-benchmark-2026-05-31.md).
Execution plan for the P0 keystone: [`superpowers/plans/2026-05-31-people-hub-and-ia-refocus.md`](superpowers/plans/2026-05-31-people-hub-and-ia-refocus.md).

Borrow DoorLoop's navigation legibility — one **People** hub + a **Properties** spine,
fewer destinations, consistent record pages — **without cloning it**. Keep Smart Intake,
review-first guardrails, and AU/Xero/commercial depth as the lead. AU-localise every US
analog (Xero not QuickBooks; PayTo/PayID/BPAY not Stripe/ACH; realestate.com.au/Domain;
Equifax/illion; RTBA/state RTAs).

**P0 — structural keystone (do first; low net-new features, unlocks the rest):**

- [x] **Owner as a first-class entity.** SHIPPED (commit `5685c90`): `Owner` + `PropertyOwner`
  models, migration `20260531_0029`, `/api/v1/owners` CRUD + property-link attach/detach,
  idempotent `scripts.backfill_owners`, and a statement-grouping parity test. Legacy
  `Property.owner_*` fields are now a backfill source only.
- [x] **Owner statement read-path cutover.** SHIPPED: `/api/v1/owners/statements` now groups
  by `Owner`/`PropertyOwner` links, keeps unlinked properties visible in an
  `Unattributed` fallback bucket, and leaves statement PDF/dispatch/provider guardrails
  unchanged. The parity test remains as the safety net for the backfilled SKJ data.
- [~] **People hub IA.** SHIPPED pending Remba review: `/people` with a live Owners
  directory (on the new API), Tenants/Vendors inline under the same hub, Prospects stub,
  `?tab=` URL state, sidebar slot, palette actions, and `G E`. Tenant, Owner, and
  Vendor records now share the same People record shell and section links, with new
  `/owners/[ownerId]` and `/contractors/[contractorId]` detail routes.
- [~] **Navigation consolidation to 7 hubs.** SHIPPED pending Remba review: sidebar is now
  Dashboard · Smart Intake · Properties · People · Work · Money · Insights (+ Settings).
  Tenants/Vendors live under **People**, Billing / Statements / Xero / Basiq group under
  the new `/money` hub, Comms is treated as **Work**, and hub alias redirects preserve
  moved-route entry points. Honours the §10.5.1 seven-item sidebar cap.

**P1 — reaches the standard (AU-correct):**

- [~] **Owner portal (read-only first).** Operator-preview foundation implemented:
  `/api/v1/owner-portal/{owner_id}` + `/owner-portal/[ownerId]` expose owner identity,
  linked properties/splits, statement totals, and no-send/no-provider guardrails.
  Owner login/account claiming, secure document share, and richer owner dashboard
  sections remain next.
- [ ] **Tenant self-serve payments on AU rails.** PayTo / PayID / BPAY / direct debit
  (Monoova / Zai / Stripe AU). Tenants pay in-portal; reconciliation stays review-first
  through the existing Basiq/Xero engine. DoorLoop's #1 value driver, AU-correct.
- [ ] **Installable PWA (mobile runway).** Operator field use (inspections / photos /
  approvals) + tenant portal on a phone, ahead of any native build.

**P2 — rounds it out:**

- [ ] Owner distributions + automatic management-fee deduction.
- [ ] Vendor portal (contractors log in, accept jobs, post updates/photos).
- [ ] Finish eSign → lease (DocuSign helper exists) + the Communications hub (templates + logs).
- [ ] Customisable reporting, a Calendar surface, and a Workflows builder.

**P3 — product-for-the-AU-market bets (low value while SKJ-internal/commercial):**

- [ ] Leasing CRM funnel: Prospect/Lead entity → application → screening → eSign → lease.
- [ ] Listing syndication (realestate.com.au + Domain) and AU screening (Equifax/illion,
  MyID/Stripe Identity). Also tracked in the big-bets list below.

**Do not copy from DoorLoop:** per-unit pricing sticker shock; autonomous tenant-facing AI
(keep review-first); shallow / non-customisable reporting; no per-unit costing; slow
payment settlement. These are places to *beat* the standard.

### Tier 1 — UX review (active, next 2-3 weeks)

- [x] Tier 1 (d) v1: URL-persistent filters across the Properties (`occupancy`, `owner_tag`), Tenants (`tenant_filter`, `q`), and Operations (`tab`, `assignee`, `maintenance_status`, `maintenance_priority`, `arrears_status`) tables. Operators can bookmark and share filtered views; filters hydrate from query string on mount and write back via `history.replaceState`. Pending Remba review.
- [x] Tier 1 (c) v1: Dashboard metric trend deltas + sparklines. `DashboardMetricCard` now accepts an optional `trend: { delta, series[], direction, label }` and renders a coloured 7-day SVG sparkline + delta badge (Stripe-style "↓ 2 vs last week"). v1 wires the trend on the Operations card from `obligationsQuery` (lower-better palette so closing more than you open shows green). Property/tenant trends defer until the read API exposes `created_at`/`deleted_at` or a dedicated `/dashboard/metric-trends` backend endpoint lands. Pending Remba review.
- [x] Tier 1 (b) v1: new generic `<DetailDrawer>` component at `apps/web/src/components/detail-drawer.tsx` (right-side panel, sticky header, primary-action footer, Escape close, body-scroll lock). Tenants table row click now opens a quick-view drawer showing contact, lease summary, and onboarding status, with "Open full record" footer link still routing to `/tenants/[id]`. Smoke coverage verifies the tenant quick-view drawer content, full-record link, and Escape close. Properties workspace already renders selected-property detail inline below the table; converting that to a drawer pattern is a larger restructure and remains TBD. Pending Remba review.
- [x] Tier 1 (a) v1: 240px fixed left sidebar with the navy `#0B1020` background, brand at top, icon + label for each module (Dashboard / Smart Intake / Properties / Tenants / Work / Billing / Insights / Portfolio QA / Settings), `G X` shortcut chip per item on xl+ screens, and a "Keyboard shortcuts" footer link. Top bar collapses to a slim right-aligned utility row (entity selector children, search, cheatsheet, notifications, user). On <lg widths the sidebar hides and a hamburger button opens a left drawer with the same nav. AppHeader toggles `body.app-shell-active` on mount so layout.tsx's body can apply the 240px gutter only on operator surfaces — auth/setup/tenant-portal pages stay full-width. Pending Remba review of the navy/sidebar contrast against the existing soft-grey content area and whether the top bar should keep stickiness now that the sidebar already anchors navigation. Supporting UX evidence for the sidebar-over-top-bar choice (item count + nesting argument, NN/g and Baymard references, Linear/Stripe/Notion/Vercel comparison) lives at [`docs/nav-pattern-research-2026-05-23.md`](nav-pattern-research-2026-05-23.md).
- [x] Tier 1 (a) follow-up: AppHeader top-right utilities now render as a single compact toolbar with the entity selector, command search, keyboard shortcuts, notifications, appearance, and profile/sign-in grouped inside one quiet container. This removes the always-visible row of separate bordered icon buttons while keeping the same operator actions available. Smoke coverage now asserts the selector and utility actions live inside the `Workspace utilities` toolbar. Pending Remba review.

### Tier 2 — strategic next bets (next 1-2 months)

- [x] Tier 2 (e) v1 backend + v2 dashboard panel: "Ask Leasium" AI Q&A surface. Backend `/api/v1/ai/ask` (c1b2a7c) takes a natural-language question + entity_id, builds a bounded context dump of the operator's properties, units, tenants, leases, obligations, maintenance work orders, and arrears cases, and calls OpenAI with a strict-JSON schema requiring citations back to source-record IDs. Citations are post-validated against the context so the model can't invent record IDs. Read-only — no provider mutations. Returns 503 when `OPENAI_API_KEY` is unset and audits every question with citation kinds (not answer body). v2 frontend adds an `<AskLeasiumPanel>` to the Dashboard with question input, four suggestion chips (lease expiries, vacancies, open work, arrears), answer card, source deep-link chips, warnings strip, and an expandable Guardrails disclosure — placed directly under Upcoming lease events. Errors (503/network) render inline. Smoke fixture mocks `/ai/ask`; backend test asserts citation validation + 503 path. Pending Remba review.
- [x] Tier 2 (f) v1: Cross-property activity feed on the Dashboard. New `GET /api/v1/activity-feed?entity_id=...&limit=N` endpoint projects the append-only `audit_action` rows for the entity into a presentation-friendly shape — actor name (joined from app_user), action_kind chip, target label (batched lookup for property/tenant/lease/invoice/maintenance/arrears/document_intake/tenant_onboarding/obligation), target deep-link href, summary (uses `tool_output_summary` when written, else synthesised), and outcome. Dashboard now renders `<ActivityFeedPanel>` between Ask Leasium and Smart Intake, grouped by Today / Yesterday / Earlier this week / Older time buckets with a 60-second background refresh. Read-only — no double-write, no separate feed table; the audit log everything else already writes is the source of truth. Backend integration tests assert audit projection, has_more pagination, and access control; smoke fixture mocks `/activity-feed`. Pending Remba review.
- [ ] Tier 2 (g): Dark mode v1 — add dark tokens to the design source of truth, apply `.dark` class via system preference + an account-menu toggle, audit the 5 most-used surfaces for contrast. Known issue to include in that pass: verify real Clerk-hosted auth widgets under dark OS/browser settings. The local sign-in/sign-up appearance object now pins light variables and Leasium card/input/button classes, but full dark-mode rollout should still re-check the live Clerk iframe/rendered states.
- [x] Tier 2 (h) v1: Keyboard shortcuts beyond Cmd+K. AppHeader now installs a global keydown listener (skipped when typing into inputs/textareas/contenteditable). `G` opens a "go to" window for 1.5s; the next letter navigates: `D` Dashboard, `I` Smart Intake, `M` AI Inbox, `P` Properties, `T` Tenants, `O` Operations (Work), `R` Contractors, `C` Comms, `F` Owner statements, `B` Billing Readiness, `N` Notifications, `Q` Portfolio QA, `S` Settings. `?` opens a Linear-style cheatsheet modal listing Global + Go-to shortcuts. `Esc` closes any open dialog. A bottom-centre toast confirms the G window is active and derives its letter legend from `SHORTCUT_NAV`. Command-search and shortcut overlays expose dialog labels, command search uses a labelled textbox, and both overlays close from backdrop click as well as close/Escape. Pending Remba review of the toast placement and the chosen letter map.

### Tier 3 — bigger bets, post internal-period

- [~] Tier 3 v1-v3: Inline-editable cells on Tenants, Properties, and Operations tables. Reusable `<InlineEditCell>` (`apps/web/src/components/inline-edit-cell.tsx`) supports both text input and select-dropdown variants (the latter via `options` prop), with the same click-to-edit / Enter or change to commit / Escape to cancel / pencil affordance / loading spinner / inline error / rollback contract. v1 wires Tenants Contact (name/email/phone) against PATCH `/api/v1/tenants/{id}`. v2 wires Properties workspace table (property name + street address with composed display) against PATCH `/api/v1/premises/{id}`. v3 wires Operations maintenance list status and priority chips (select variant) against PATCH `/api/v1/maintenance/work-orders/{id}`. v3.1 adds a short undo toast for Operations status/priority changes and fixes the select editor so the newly selected value is the committed value. All three flows use optimistic React Query cache updates with rollback on failure. Drawer + full-record editing still ship as later follow-ups. Pending Remba review of pencil affordance, chip vs. text display, undo-toast placement, and whether status/priority should keep a coloured badge while clickable.
- [x] Tier 3 v1 saved views: operators can name and recall filter combinations on Tenants, Properties, and Operations. New `apps/web/src/lib/saved-views.ts` helper persists views per surface in `window.localStorage` (`leasium.saved_views.{surface}`); `apps/web/src/components/saved-views-menu.tsx` provides the dropdown chip rendered above each table (Bookmark icon, active-view name when filters match, "Custom view" when they don't, "Saved views"/"No saved views" otherwise). Menu offers Apply / Rename / Delete per view and "Save current as…" inline. Filters track the same keys as the URL-persistent layer shipped in Tier 1 (d) — tenant_filter+q for Tenants, occupancy+owner_tag for Properties, tab+assignee+maintenance_status+maintenance_priority+arrears_status for Operations. 2026-05-31 follow-up polish lifted the menu trigger, row actions, save form, and close control to the 44px target baseline while preserving localStorage and URL-filter semantics. v1 is local-only; promote to DB-backed `saved_view` table once a second operator comes online. Pending Remba review.
- [x] Tier 3 v1 multi-view for Properties: Table (existing) and Board (new) views toggleable from a tab strip above the property list, with `?view=board` URL persistence alongside occupancy + owner_tag filters. Board view groups the filtered property set into occupancy buckets (Leased / Leased internal / Partial / Vacant / No units) — each card shows the property thumbnail, name, address, and next-expiry chip; clicking a card selects the property like the table row does. Map and Calendar variants noted as v2 follow-ups (map needs a library decision, calendar's natural surface is upcoming lease events on the Dashboard rather than properties themselves). Smoke coverage verifies table/board toggle and URL persistence. Pending Remba review.
- [x] Tier 3 v1: AI inbox processor (Re-Leased Credia Action equivalent). New `/inbox` route + `POST /api/v1/ai/triage` endpoint. Operator pastes a message body; OpenAI returns a strict-JSON classification (kind, confidence, summary, suggested action, suggested target kind, key facts, warnings). Kinds: maintenance_request / payment_or_arrears / lease_change / tenant_contact / vendor_or_contractor / general / spam_or_noise. The classification card shows a deep-link "Take it from here" button pointing at the right Leasium surface (Operations, Tenants, Properties, etc.) so the reviewed workflow takes over. v1 is read-only — no records are created automatically. Backend audits body length + kind + confidence (not the body itself). Sidebar nav + G+M shortcut wired. Verified on Mac-side backend and Playwright smoke. Pending Remba review.
- [x] AI inbox v2: promote classifications into Leasium drafts. `POST /api/v1/ai/triage` now builds a compact entity index (properties, tenants, active leases) and asks OpenAI to return `suggested_property_id` / `suggested_tenant_id` / `suggested_lease_id` if it can confidently match the message to a record; returned ids are validated against the index server-side (invented uuids are dropped). New `POST /api/v1/ai/triage/promote` takes a reviewed kind + operator-confirmed property/tenant/lease and creates a draft: `maintenance_request` → `MaintenanceWorkOrder` in `requested` status; `payment_or_arrears` → `ArrearsCase` in `active` status (requires matched tenant, 422 otherwise); `lease_change` → synthesises a text/plain `StoredDocument` from the message body + a `DocumentIntake` row in `uploaded` status so the existing Smart Intake review flow takes over. No provider mutation — the draft sits in its initial review state until the operator approves the next step from inside the target surface. Audit log records kind + summary length + target table. Frontend `/inbox` shows a Promote-to-draft panel below the deep-link CTA when the classified kind is actionable, with property/tenant/(lease) dropdowns pre-filled from the AI's suggested matches and operator override; clicking Promote routes the operator into the new draft's detail page. Pre-existing latent bug fixed in `apps/api/routers/ai.py`: `assert_entity_role` was being called with the wrong arity (`(user, entity_id, roles)` instead of `(session, user, entity_id, roles)`) on `/ai/ask` and `/ai/triage`; all three call sites now use the correct 4-arg form. Verified on Mac-side backend and Playwright smoke. Pending Remba review.
- [x] AI inbox v2.1: pre-extract lease-change facts at promote time. New `stewart/ai/lease_change.py` runs a strict-JSON OpenAI call that returns the proposed change shaped to the existing `DocumentIntakeExtraction` keys (parties / properties / key_dates / money_amounts / proposed_actions / summary / warnings). On promote with `kind=lease_change`, the router builds a compact lease snapshot (current expiry / current rent / next review) from the operator-matched lease, runs the extractor, and stamps the result onto the new `DocumentIntake` row: status becomes `ready_for_review` when confidence ≥ 0.5, `needs_attention` below that. Soft-fail when `OPENAI_API_KEY` is unset or the extractor raises — the intake still gets created in `uploaded` status with the error recorded in `review_data["extraction_error"]`, no 5xx. Promote responses return entity-aware Smart Intake review links (`/intake?entity_id=...&review=...`) so the inbox handoff opens the exact review row even if the operator last worked in another entity. Zero frontend change required because the existing Smart Intake review UI already renders the populated groups. Audit log records the extraction outcome (`extracted` / `soft_failed` / `skipped`) without persisting the extracted values.
- [x] AI inbox v2.2: contractor/vendor matching + promote to contractor directory. `/ai/triage` entity index now includes contractors and the response gains `suggested_contractor` (mirrors property/tenant/lease match validation — invented uuids dropped). `vendor_or_contractor` joins the promotable kind list; `InboxPromoteRequest` gains `contractor_id`. Promote branch: if `contractor_id` supplied → no draft created, just deep-link to `/contractors` (existing directory entry); if absent → new `stewart/ai/vendor_intake.py` extracts name / company / email / phone / categories / notes / confidence from the message body, and a new `Contractor` row is created at priority=3 (backup) with those fields. Categories come from the same 10-value `MAINTENANCE_CATEGORIES` enum the maintenance categoriser uses, so the new directory entry plugs cleanly into the existing maintenance-dispatch matcher. Soft-fail when the API key is unset or the extractor raises — operator still gets a draft Contractor seeded from the triage summary, no 5xx. Frontend `/inbox` promote panel swaps the property/tenant/lease pickers for a single Contractor dropdown when kind is `vendor_or_contractor` (empty option = "Create new contractor"). Button label flips between "Open contractor profile" (matched) and "Add to contractor directory" (unmatched). Verified on Mac-side backend and Playwright smoke. Pending Remba review.
- [x] AI inbox v2.3: tenant-contact promote. `tenant_contact` now joins the promotable kind list. A new read-only `stewart/ai/tenant_contact.py` extractor proposes contact-name, contact-email, contact-phone, and billing-email updates only; legal/trading identity changes deliberately stay out of scope. New `POST /api/v1/ai/triage/tenant-contact-preview` returns current vs proposed field rows, confidence, warnings, and guardrails for operator review. The `/inbox` promote panel lets the operator prepare proposals, tick the fields they trust, and apply only selected fields through `POST /api/v1/ai/triage/promote` with `kind=tenant_contact`. The promote branch requires a matched tenant, ignores unchanged/empty fields, writes an audit row, and records a compact `tenant_metadata.ai_inbox_contact_promotions[]` history entry. No email/provider mutation. Frontend smoke covers the prepare -> apply handoff.
- [x] Tenant detail Delete button (2026-05-24). SecondaryButton next to "Edit profile" on `/tenants/[id]` uses `window.confirm()` (matching the contractor remove pattern), warns about active leases that will lose their tenant link, hits the existing `DELETE /api/v1/tenants/{id}` (soft delete via `deleted_at`), invalidates caches, redirects to `/tenants` on success. Smoke coverage confirms the warning and redirect.
- [x] Smarter Unit picker on Send invite (2026-05-24). `/tenants` Send invite form adapts to the property's unit count: **0 units** hides the picker and the submit handler auto-creates a `TenancyUnit` labelled "Main premises" before creating the lease; **1 unit** auto-selects and shows as a non-interactive chip; **2+** keeps the dropdown required. The lease editor in property-workspace.tsx was surveyed but doesn't need the same logic (operator always opens it from a specific unit row). Smoke coverage confirms the one-unit chip and zero-unit auto-create request before portal invite send.
- [x] Residential property type + contextual ABN (2026-05-24). Alembic migration `20260524_0025` adds `residential` to the Postgres `property_type` enum (SQLite no-op since enums store as TEXT). Lease intake schema enum updated. Frontend `PropertyType` union and property-workspace zod schema + label map both gain "Residential". `TenantLeaseContextRead` projection now carries `property_type`. Tenant detail edit form + read-only Profile panel hide Trading-as + ABN when any of the tenant's leases is on a residential property — residential rentals don't carry those fields the way commercial business tenants do. Smoke coverage confirms the residential edit form hides business identity fields.
- [~] Tenant portal soft-switch claim gate (2026-05-24). Closes the magic-link weakness on `/tenant-portal/{token}`. Alembic `20260524_0026` adds `tenant_onboarding.token_consumed_at`. `_portal_scope` refuses tokens where `token_consumed_at IS NOT NULL` with HTTP 410 Gone — every token-scoped data endpoint flows through this helper so they all gate at once. `claim_tenant_portal_account` is the only path that passes `allow_consumed=True` and stamps consumption on first successful claim; a consumed-token reclaim is only allowed for the same Clerk user with prior history (active / revoked / unlinked). New public `GET /api/v1/tenant-portal/invites/{token}/preview` returns the minimum-viable context for the unauthenticated gate (property name, address, tenant display, expiry, claimable boolean). Frontend `/tenant-portal/{token}` renders a full-page claim gate when no account portal is linked yet — preview + Clerk sign-in/sign-up. After Clerk auth a top-level mutation auto-fires the claim and the existing portal content takes over. Disabled the unauthenticated `getTenantPortal(token)` call entirely; every data read flows through the account-scoped endpoint post-claim. Tenant invite email copy also refreshed (subject "Set up your tenant portal for {property}", CTA "Sign in to continue", postscript explains the single-use link).
- [x] Tenant portal activity feed (2026-05-25). Full tenant portal side rail now includes a Recent Activity panel derived from existing portal data: onboarding invite/submission, lease signing, lease question ask/answer events, document uploads, maintenance request history, contact change requests, and notification preference updates. The read-only operator preview now mirrors the derived feed and exports the activity rows in the preview CSV, giving local browser proof without creating a portal account. This is tenant-scoped and read-only; no new provider mutation, feed table, or backend dependency was added.
- [~] Tenant portal contact visibility + reviewed tenant edits (2026-05-25). Full tenant portal side rail now shows the tenant's confirmed legal/contact/billing details after the full portal unlocks. Tenants can request changes to contact name, contact email, phone, and billing email; the request is stored as tenant metadata, appears back in the tenant portal and operator portal preview as in-review/applied/dismissed status, blocks duplicate pending submissions, appears on tenant detail under `Tenant requests`, and only mutates the tenant record after an operator clicks `Apply request`. Operators can also `Dismiss` duplicate or incorrect requests without mutating the tenant record. Legal/trading identity changes remain out of tenant self-service scope.
- [~] Tier 3 v1 mobile audit pass: surveyed Dashboard / Operations / Notifications / Tenants / Properties / Tenant portal at <480px and 480-1024px viewport widths. Fixes shipped this pass: (1) AppHeader utility row now wraps — the entity selector drops to its own full-width row on mobile so it doesn't crush the icon cluster, and the Keyboard cheatsheet button is hidden below sm (operators on phones don't use keyboard shortcuts). (2) Dashboard metric grid gains intermediate breakpoints (`sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6`) so the 6 cards never get stuck two-wide-three-rows on a laptop. (3) Properties workspace table swapped `overflow-hidden` for `overflow-x-auto` with `min-w-[640px]` so it scrolls horizontally on phones instead of clipping. Operations / Notifications / Tenant portal already used `md:`/`xl:`/`flex-wrap` patterns so they reflow correctly without changes — confirmed during audit. Pending Remba review.

### Automation backlog — driving "minimise humans in the loop"

The 2026-05-23 [automation strategy](automation-strategy-2026-05-23.md) frames every item below: the platform's purpose is to take work *off* the operator. Each automation drafts and queues; the operator approves. The provider-mutation guardrail in [`CLAUDE.md`](../CLAUDE.md) is non-negotiable for every item in this list.

**Near-term (next 1-3 sprints):**

- [~] **Scheduled comms loop v1 + v2 + dispatch.** `/api/v1/comms/queue` now returns draft communications for arrears, insurance expiry, lease renewal, inbound email/SMS, compliance obligations, rent reviews, and maintenance forwarding drafts. SendGrid inbound email attachments now store as source documents and create Smart Intake review rows tied back to the inbound message/attributed tenant; when OpenAI is configured they pre-extract into review-ready Smart Intake rows, and extraction errors soft-fail without losing the stored attachment. The comms queue detail and CSV call out attachment counts routed to Smart Intake. `POST /api/v1/comms/dispatch` sends the operator-approved draft via the matching channel (SendGrid for email candidates, Twilio for `inbound_sms`; Approve click = explicit provider-mutation approval). `POST /api/v1/comms/dismiss` records the deferral. `/comms` operator page renders queue-generated freshness with a manual refresh action, a local `comms-queue-review-{date}.csv` export from already-loaded candidates, session-local remaining/settled counts that reconcile against refreshed queue candidates, filter-band progress summary, each candidate as an editable card with channel badges, edited-draft badges plus pending-safe reset-to-staged-draft action, post-action badges, due/drafted timing chips, Approve / Dismiss, provider-and-recipient dispatch receipts plus deferred-until dismiss receipts announced as statuses, post-action field locking and locked-state guidance, phone-recipient SMS handling, accessible inline approval blockers for missing recipient/subject/body, per-kind filter tabs with a visible "showing X of Y" summary for narrowing the working queue without changing global counts, and scoped keyboard review flow (`j`/`k` or arrows move focused draft rows; Enter jumps into the first editable field; typing inside fields is not intercepted). The CSV export does not call dispatch, dismiss, evidence upload, SendGrid/Twilio sends, provider-history writes, candidate settlement, queue mutation, or provider refresh paths. Pending Remba review of the queue density, keyboard affordance, severity-tier copy, and whether the CSV handoff is plain enough for daily operator review.
- [x] **DocuSign provider helper v1.** `stewart/integrations/docusign.py` + four `DOCUSIGN_*` settings now soft-skip with `not_configured` until credentials are set, then use JWT grant, envelope create, and completed combined-PDF download for the attached lease document. Setup steps in `docs/deployment.md`; remaining work is live provider-console verification.
- [x] **Evidence attach on `/comms` compliance candidates.** Compliance obligation cards now have a dedicated Attach evidence panel. Primary CTA: prominent "Upload via Smart Intake" Link to `/intake` (recommended). Secondary muted button: "Or attach a file manually" — opens a hidden file picker, uploads via the existing `uploadDocument` endpoint with `category=other` attributed to entity + tenant + source obligation. Manual uploads now back-attribute the stored document id into `obligation_metadata.evidence_document_ids[]`, append compact `evidence_history[]`, and stamp the document metadata with `source=manual_comms_evidence_upload` plus the source obligation id. Inline copy tells operators the manual fallback is linked to the compliance obligation for audit follow-up. Backend coverage proves the source document and obligation evidence history link; smoke coverage now exercises the compliance card, Smart Intake handoff, manual upload receipt, and reviewed SendGrid approval path without provider mutation when SendGrid is unconfigured.
- [x] **In-app comms badge on the sidebar Work nav.** New `/api/v1/comms/queue/counts` lightweight endpoint + sidebar badge that shows `urgent` count in danger-red when > 0, otherwise total in muted white. AppHeader reads localStorage `leasium.entity_id`, can refresh from same-tab entity-change events, queries counts on mount with 60s staleTime, and `/comms` emits that event plus refreshes the count after queue refresh, approve, or dismiss actions. Satisfies the "compliance reminders fire as in-app notifications" intent while Comms is folded under Work in the trimmed sidebar. Backend counts coverage proves urgent/by-kind totals, and browser smoke verifies the Work nav label exposes the comms queue count.
- [x] **Inbound SMS via Twilio webhook.** New `POST /api/v1/comms/webhooks/twilio-inbound?entity_id=...` accepts Twilio's PascalCase form payload (`From`, `To`, `Body`, `MessageSid`, `FromCountry`), verifies `X-Twilio-Signature` when `TWILIO_AUTH_TOKEN` is configured, persists as `inbound_message` with `channel="sms"` + `provider="twilio"`, attributes by digits-only phone-number suffix match against `tenant.contact_phone` (handles `+61400111222` ↔ `0400111222` ↔ `+61 400 111 222`), and runs the same `/ai/triage` classifier as the email path when `OPENAI_API_KEY` is set. Surfaces in the comms queue as a new `inbound_sms` candidate kind with a short SMS-appropriate draft body (no subject, no quoted snippet) and `recipient_phone` populated instead of `recipient_email`. Existing email-channel behaviour unchanged. Setup steps in `docs/deployment.md`.
- [x] **Inbound email AI classification v1.** When `OPENAI_API_KEY` is set, the inbound webhook runs the existing `/ai/triage` classifier against each inbound message body and stamps `classification_kind`, `_confidence`, `_summary`, `_target_kind` on the `inbound_message` row. Soft-fails when the key is missing or the call errors — the row is still persisted. The comms queue's `inbound_email` candidate surfaces the classification in the detail line ("AI: payment or arrears (84%)") and elevates severity for `payment_or_arrears` (danger) and `maintenance_request` (warning). Audit log records the kind only, never the body, matching the existing /ai/triage guardrail.
- [x] **Compliance obligation tracking v1.** Reuses the existing `Obligation` model — its `insurance`, `bank_guarantee`, `make_good`, and `compliance` categories are all swept by a new `compliance_obligation` candidate kind in the comms queue. Surfaces obligations in `upcoming`, `due_soon`, or `overdue` status whose `due_date` is within 45 days (or already overdue). Drafts a tenant-facing reminder when a lease + tenant attach to the obligation; otherwise leaves recipient empty so the operator can route it manually. Severity steps with overdue/due_soon/upcoming. Dispatch + dismiss support the new kind, stamp `comms_dismiss` metadata on the obligation, and the queue scanner now honors the deferral/next-eligible stamp so reviewed compliance candidates do not immediately resurface. No migration needed — Obligation already covers compliance.
- [x] **Inbound email parsing v1.** New `inbound_message` table (migration `20260523_0023`). `POST /api/v1/comms/webhooks/sendgrid-inbound?entity_id=...` parses SendGrid Inbound Parse form data, attributes the message to a tenant by matching the from-address against `contact_email` / `billing_email`, and persists the row. When `SENDGRID_INBOUND_SECRET` is configured, the webhook rejects missing/wrong shared secrets before persisting; SendGrid can pass the value as a query token or custom header. Surfaces in the comms queue as an `inbound_email` candidate the operator reviews and replies to via the same dispatch path. Attachments route to Smart Intake review rows and pre-extract when `OPENAI_API_KEY` is configured. External SendGrid Inbound Parse + DNS MX setup remains a live environment follow-up.
- [x] **Maintenance status forwarding automation.** Work-order Activity prepares review-only "tenant → contractor" and "contractor → tenant" local copy drafts from the latest visible timeline rows, and the completion review packet has a local `maintenance-completion-review-{workOrderId}.csv` export covering closeout evidence counts, recipient review statuses, billing handoff, latest activity, open review items, and forwarding draft readiness from already-loaded page state. The same latest tenant-visible and contractor-visible timeline rows now surface in `/api/v1/comms/queue` as `maintenance_contractor_forward` and `maintenance_tenant_forward` candidates, `/comms` labels them as contractor/tenant forwards with a work-order handoff, and Approve routes through the existing explicit Comms dispatch path before any SendGrid email is attempted. Queue fetch and CSV export stay read-only; no portal, Xero, billing, closeout/review metadata, or provider-history mutation runs without the operator's Comms approval.

**Medium-term (next 1-2 quarters):**

- [x] **SMS outbound dispatch wired through Twilio Messaging.** `/api/v1/comms/dispatch` now branches by kind: `inbound_sms` candidates route through Twilio Messaging (new local `_send_comms_sms` helper mirroring `_send_comms_email`), everything else stays on SendGrid. Soft-fails when Twilio is not configured or when the recipient phone isn't E.164. Closes the SMS reply loop — operators can now Approve an inbound SMS candidate and actually send a reply via SMS, not email. The `/comms` card now preserves `recipient_phone`, posts `recipient_phone` on approval, hides the email-only subject editor for SMS, labels the review copy as Twilio/SMS, and shows a 160-character SMS body guide before approval. The dispatched status updates `inbound_message.processed_at` so the candidate clears from the queue. Audit log records channel + tool_name = `twilio.{provider}`.
- [x] **Annual rent reviews v1 — comms queue candidate.** Data-model audit done: existing `Lease` model already has `annual_rent_cents`, `next_review_date`, and a `lease_metadata` JSONB; no migration needed. v1 stores the increase formula in `lease_metadata["rent_review"] = {kind, increase_pct, cap_pct, formula_notes}`. New `rent_review` candidate kind in the comms queue scans active leases with `next_review_date` within 60 days (or overdue), computes the new rent for `kind: "fixed_pct"` (with optional `cap_pct`), and drafts a tenant-facing notice referencing current rent, proposed new rent, and the formula label (e.g. "+3% fixed increase"). Severity: overdue → danger, ≤30 days → warning, ≤60 days → info. Leases without a formula still surface with detail "needs increase rule" so the operator sets one before approving. Dispatch + dismiss endpoints accept the new kind through the existing lease-targeted branch, stamp `comms_dismiss` metadata on the lease, and the queue scanner now honors that metadata so reviewed rent-review candidates do not immediately resurface. v2 adds CPI (needs an external rate feed) and market-based (needs comps data) formula kinds. v3 adds a dedicated batch-approval surface — today the operator approves one-by-one in `/comms` which is fine for SKJ's portfolio scale. v4 generates the new `charge_rule` row on dispatch so the rent increase actually applies after notice — today it stays as a notice only.
- [x] **Owner monthly statements v2 — operator-facing `/statements` page.** New route renders the v1 JSON: month selector (defaults to previous calendar month, matching backend default), portfolio totals strip (owners count, invoiced, paid, outstanding), one `SectionPanel` per owner with trust/trustee header, owner-level invoiced/paid/outstanding metric grid, and a per-property table inside the card (Property / Invoiced / Paid / Outstanding / Invoices, sorted invoiced-desc with overdue amounts in red). Empty state explains that approved invoices land here. Inline footnote reminds the operator that paid totals come from Xero reconciliation receipts and that outgoings + management fees are a future roll-up. Sidebar nav gains a Statements entry between Insights and Portfolio QA with a `G F` shortcut. The local UI has since grown readiness, finance checklist, signoff, dispatch review, and PDF pack actions while keeping provider dispatch review-first.
- [x] **Owner monthly statements v1 — backend compile endpoint.** Data-model audit done: `Property` already has 11 owner-identification fields (`owner_legal_name`, `owner_abn`, `trustee_name`, `trust_name`, `invoice_issuer_name`, `billing_contact_name`, `billing_email`, `invoice_reference`, `ownership_split`, `owner_gst_registered`, `xero_contact_id`); no new `owner` table needed. v1 ships `GET /api/v1/owners/statements?entity_id=...&month=YYYY-MM` that groups properties by an owner-identity tuple (`owner_legal_name` + `trustee_name` + `trust_name` + `invoice_issuer_name`, case-folded), then for each owner aggregates approved InvoiceDraft rows whose `issue_date` falls in the target month. Paid totals come from `invoice_metadata.paid_cents` (written by Xero reconciliation). Outstanding = `invoiced - paid` floored at zero. Properties with no owner identification fall into a single "Unattributed" bucket. Statements sort by invoiced desc; properties within each statement also sort by invoiced desc. Defaults to previous calendar month when `month` is not supplied. Read-only endpoint behavior is covered locally; accountant formatting review remains on the richer PDF export item.
- [x] **Maintenance categorisation v3 — AI classification panel on the work-order detail.** New `AiClassificationPanel` slot between Approval and Contractor on the work-order page. Empty state explains the categoriser; once classified it renders the category badge with confidence pct, the AI summary in a quoted block, a Same-day badge when `is_urgent`, the classification timestamp, any warnings list, and the suggested contractor card (name + email + phone) sourced from the metadata stamp. Apply-to-contractor button copies the suggested contractor name onto the work order's `contractor_name` field with one click; an "Applied" badge replaces the button when the contractor on the work order already matches the suggestion. Falls back to a "no contractor matched the category" hint with a link to `/contractors` when no directory match. Re-classify is one click. Soft-fails inline with the 503 message when OPENAI_API_KEY is unset. The dispatch (Approve / Send brief to contractor) remains the existing reviewed flow — v3 only hands the operator the suggestion. Focused smoke coverage now clicks Classify with AI, renders the category/confidence, Same-day badge, warning, suggested contractor name/email/phone, verifies the no-dispatch guardrail copy, applies the suggestion, and sees the Applied state. Future UX tuning can still revisit panel placement, whether Apply should copy email/phone too, and whether low-confidence suggestions should withhold the button.
- [x] **Maintenance categorisation v2 — AI classifier + contractor suggest.** New `stewart/ai/maintenance.py` with the 10-category enum (electrical / plumbing / hvac / locks / structural / appliance / cleaning / pest / urgent / other) mirroring `CONTRACTOR_CATEGORIES`. OpenAI strict-JSON schema returns `{category, confidence, summary, is_urgent, warnings}`. New `POST /api/v1/maintenance/work-orders/{id}/classify` runs the categoriser against the work-order title + description, then queries the contractor directory for any contractor whose `categories` overlaps with the AI's chosen category (sorted priority asc) and picks the top match. Stamps `work_order_metadata.ai_classification = {category, confidence, summary, is_urgent, warnings, suggested_contractor_id, suggested_contractor_name, classified_at, model_response_id}`. Soft-fails with 503 when `OPENAI_API_KEY` is unset. Audit log records category + confidence + suggested contractor id (never the description body). The actual dispatch remains the existing reviewed flow; v2 just hands the operator the suggestion. The v3 work-order panel remains tracked separately.
- [x] **Contractor directory v1.** New `contractor` table (migration `20260523_0024`) — per-entity, fields: `name`, `company_name`, `categories: list[str]` (electrical / plumbing / hvac / locks / structural / appliance / cleaning / pest / urgent / other), `email`, `phone`, `service_radius_km`, `priority` (1=preferred, 2=normal, 3=backup), `notes`, `contractor_metadata`. Full CRUD at `/api/v1/contractors` (list / create / patch / soft-delete). Operator-facing `/contractors` page renders the directory sorted by priority asc + name asc, with an Add form (category pill multi-select), per-contractor card showing categories as chips + priority badge + service-radius badge + notes, a local `contractor-directory-readiness.csv` export for contact/category/AI-suggest readiness, and a Remove button with confirm. The CSV export does not send contractor email/SMS, run maintenance AI classification, assign work-order contractors, create/update/delete contractors, write provider history, or dispatch receipts. Sidebar nav entry between Work and Billing with `G R` shortcut. AI categorisation and the work-order suggestion panel are tracked in the v2/v3 maintenance categorisation items.
- [x] **Tenant-uploaded lease auto-match runway v1.** Tenant portal `lease` uploads now auto-promote supported PDF/DOCX/TXT/MD files into Smart Intake with tenant/onboarding/lease scope and a review-first auto-match guardrail. When OpenAI is configured, tenant-uploaded Smart Intake files are also extracted in the background so operators get ready-to-review facts instead of a blank intake row. Extracted lease uploads now stamp a `lease_auto_match` recommendation against the scoped lease, separating matched fields, differences, and missing fields for review; Smart Intake surfaces that recommendation in the review panel with the no-mutation guardrail visible. Operators can now **Accept match** on a matched tenant-uploaded lease, which applies the intake, links the document to the existing lease, stamps onboarding signing as completed by tenant upload, appends an activation-review audit row on the onboarding, and queues the existing activation-review handoff for pending leases without mutating lease status/register values or creating a `LeaseIntake`. Accept-match is deliberately narrow: tenant-portal source only, onboarding-scoped only, blocked when differences or missing fields remain, blocked unless document/onboarding scope matches the accepted lease, and blocked while an active DocuSign envelope is still queued/sent/delivered for the same onboarding. Tenant-upload activation reviews now also surface in the tenant lifecycle comms queue; tenant detail labels the signed state as `Tenant upload accepted`, links back to the entity-aware source Smart Intake review, and shows a tenant-upload-specific activation success notice so accepted uploads get the same operator-visible final-review prompt as completed DocuSign envelopes without looking like generic signatures.
- [x] **Tenant-uploaded insurance auto-update v1.** Tenant portal `insurance` uploads now auto-promote into Smart Intake and, when OpenAI is configured, auto-extract in the background for operator review while preserving tenant-portal source, candidate, scope, and no-mutation guardrail metadata through extraction. Smart Intake labels those review rows as tenant portal uploads with insurance-specific handoff detail, and Dashboard/Operations/Insights/Property Workspace/Portfolio QA/Billing Readiness review links now include both `entity_id` and `review` so they open the exact intake even when another accessible entity was selected previously. When an operator reviews/applies a scoped `insurance_certificate`, Leasium creates/links the renewal obligation and updates the tenant metadata with `insurance_confirmed`, expiry, source document/intake ids, and review history; if the document has a lease scope, the metadata update follows the lease tenant and corrects any stale document tenant id. Tenant detail now surfaces the confirmed insurance expiry, Smart Intake source, and explicit `Open Smart Intake review` / source-document links. Upload/extraction itself still does not mutate tenant metadata or recategorize the stored document; AI classification is retained as proposed metadata until review/apply.
- [x] **Inbound email attachment Smart Intake v1.** SendGrid inbound parse can route tenant email attachments into Smart Intake, optionally pre-extract them when OpenAI is configured, labels those review rows as inbound email attachments with the email subject and no-mutation guardrail visible to operators, and exposes a Comms queue handoff back to Smart Intake for inbound attachment replies. Backend coverage verifies inbound webhook routing, attachment promotion, and shared-secret handling; smoke coverage verifies the Smart Intake filter, CSV, review labels, extracted policy facts, and no-mutation guardrail. Remaining follow-up is live MX/SendGrid setup once the shared secret is configured.
- [~] **Bank-feed reconciliation v1.** Basiq (AU) bank-feed OAuth → AI matches each transaction against expected charge rules → operator approves a batch. Removes the majority of reconciliation hours. **v1 shipped (review-first):** a Basiq adapter that soft-skips until `basiq_enabled` + `basiq_api_key` are set; `POST /api/v1/basiq/reconciliation-preview|apply` that REUSE the Xero reconciliation engine (one guarded, local-metadata-only apply path) with an explicit per-row `approved_idempotency_keys` gate and explainable reference/amount/due-date matching; and a Bank feed panel in Settings to review imported transactions and approve matches. Inert with no real money movement. **Live connection foundation now shipped:** a Basiq server-token + accounts/transactions fetch (credit + posted + AUD only) behind the same soft-skip, connect/status/revoke routes, and a Settings connect UI (migration `20260531_0028`, `basiq_connection` table). It auto-fetches once a Basiq developer app + `BASIQ_API_KEY` are configured; the fetch is read-only and the whole surface is inert until then.
- [ ] **Trust account compliance reports** *(future-audience bet — NOT applicable to SKJ today; reclassified 2026-05-31 after research)*. Statutory AU trust-account reconciliation is an obligation of licensed managing **agents** holding third-party money in trust (Property and Stock Agents Act NSW; Estate Agents regs VIC; etc.). SKJ is a direct commercial **owner** receiving its own rent — no agency, no trust account — and the data model has no trust ledger (no held / disbursed / opening-closing-balance concepts). Blocked-by: Leasium serving licensed agents holding third-party trust money + a real trust-ledger data model (held balances, disbursement runs, bond lodgement) + accountant/auditor sign-off on the statutory format. Revisit with the residential/agency expansion.
- [ ] **Folder-watch document intake** *(parked 2026-05-23, not priority — operators upload through Smart Intake directly today; revisit when Smart Intake volume per-week justifies the OAuth + polling worker investment, or when a specific cloud provider becomes the entrenched filing system for a portfolio)*. Dropbox / Google Drive / OneDrive OAuth + polling worker → drops new files into Smart Intake automatically.
- [x] **DocuSign integration runway v1.** The operator-approved **Send lease pack** API now enforces an attached lease document server-side, calls DocuSign with the scoped lease document, records a DocuSign receipt under lease-pack delivery data, stamps active envelope metadata under `lease_agreement.signing`, and blocks both duplicate operator sends and tenant-side Leasium click-signing while a DocuSign envelope is queued/sent/delivered. The configured provider helper performs JWT grant + envelope create with mocked unit coverage, and `POST /api/v1/tenant-onboarding/webhooks/docusign` now requires the configured shared secret, records provider events, marks completed envelopes signed idempotently only from an active DocuSign signing state, downloads the completed combined PDF, and stores it as a signed lease document without duplicating on replay. Declined/voided/failed signing states are visible as DocuSign attention states on tenant detail without marking the lease signed or retaining a PDF; operators can use **Send again** to create a fresh envelope from the attached lease while preserving lease-pack history. Completion creates an activation-review marker only; the tenant-detail **Activate lease** action explicitly activates pending leases and stamps activation metadata, with the `Pending -> Active` transition and no-auto-activation guardrail visible before the action, and the backend now rejects activation if that explicit review marker is missing. Settings now reports DocuSign credential/webhook readiness, warns specifically when `PUBLIC_API_URL` is the remaining Connect blocker, shows the Connect webhook URL when available, and can copy/download a review-only DocuSign provider setup packet for the external-console handoff. Live provider-console verification remains tracked in the deployment and production-smoke docs rather than as unfinished local runway code.
- [x] **Inspection report intake v1.** Smart Intake extraction now recognises `inspection_report` documents and returns reviewed `inspection_findings`; the review panel can filter inspections and edit/approve/ignore finding rows. Applying a reviewed inspection report creates requested maintenance work orders with the source document, optional photo document ids, property/unit/tenant/lease scope, confidence/source metadata, and a no-dispatch/no-provider guardrail. No contractor email/SMS, provider history, billing draft, Xero action, or external mutation runs from upload/extraction/review; work orders are created only after Apply. Backend tests cover work-order creation and cross-entity photo guardrails, and smoke coverage applies inspection findings through Smart Intake into Operations. Pending Remba review of finding row labels, whether photo evidence should be visible inline, and whether these should be called "drafts" or "requested work orders" in the UI.

**Big bets (post internal-first period):**

- [ ] **Multi-tenancy / Organization layer (turn Leasium into a multi-company SaaS).** Today Leasium is implicitly single-tenant — SKJ is the unspoken root and every property, unit, lease, owner, tenant, and operator user belongs to it directly. To let other property managers sign up and run their own isolated Leasium, introduce a new top-level **Organization** (a.k.a. Company / Workspace) entity that sits above everything; SKJ becomes Organization #1. **Naming guardrail:** reserve "tenant" for renters and call the SaaS-customer layer **Organization/Company** — "tenant" is already taken by the rental domain, so reusing it for the account layer guarantees confusion. **Scope of work:** (1) add `organization_id` to every org-owned table and thread it through every query, API route, and auth check so Company A can never read Company B's data; (2) org provisioning/signup + per-org team membership and roles; (3) a platform / super-admin view for Leasium-the-operator to see and manage all orgs (distinct from each org's own scoped workspace — these are the two "global views"); (4) per-org billing/plans if charging. **Not the same as the Owner entity** (DoorLoop refocus P0): Owner is a record *inside* a company (the landlords a PM manages); Organization is the company itself — different layers, don't conflate them. **Cheap-now / expensive-later note:** retrofitting tenancy onto a live DB is painful, so even while signups stay closed it's worth landing the `organization_id` scoping early as groundwork rather than as a big-bang migration later. Effort: large — multi-month retrofit across the whole data model + new org/admin/billing surfaces. Blocked-by: the explicit decision to go multi-company SaaS (conflicts with the internal-first-6-months stance), so deferred until after the internal period.
- [ ] **Native mobile apps (iOS + Android).** Operator-side app for field workflows (inspections, photo capture, on-the-go approvals) and a separate tenant-side app for portal access from a phone. Today's responsive web covers most operator needs but Re-Leased and AppFolio both have years-of-mature native mobile inspection tooling. Effort: 3-6 months once a mobile engineer is on the team. Blocked-by: hiring + design system mobile spec.
- [ ] **Contractor marketplace + payments.** Beyond the contractor directory in the maintenance-categorisation entry above, build a marketplace surface: contractor profiles, ratings, response-time stats, on-platform messaging, and Stripe-backed payment processing so work-order invoices clear without leaving Leasium. Re-Leased and AppFolio both have this. Effort: 2-3 months. Blocked-by: confirm whether payment processing is a priority for SKJ's commercial portfolio (typically B2B EFT, not card payments) or whether the residential rental side is where this lands.
- [ ] **Listing-platform integrations (realestate.com.au + Domain).** Vacancy listings auto-publish to AU portals from Leasium. Sits between the vacancy marketing copy entry (which drafts the listing) and the applicant triage entry (which ingests responses). Needs platform partner agreements. Effort: 2-4 weeks once partner access is approved.
- [ ] **Applicant screening (credit + identity + reference checks).** When applicant data lands from listing platforms, integrate Equifax / illion (AU credit), MyID / Stripe Identity (ID verification), and previous-landlord reference workflows. AppFolio bundles this natively; Re-Leased relies on third-party integrations. Effort: 4-6 weeks per provider. Blocked-by: SKJ commercial portfolio rarely needs this; revisit when residential rental is in scope.
- [ ] **Multi-currency + multi-jurisdiction depth.** Today: AUD + AU compliance only. Future: NZD, USD, GBP currency support on charges/invoices, plus jurisdiction-specific lease compliance (US state-by-state, UK ASTs, NZ RTA). Re-Leased operates across NZ/AU/UK/US; AppFolio is US-only. Effort: 2-3 months for first expansion currency. Blocked-by: confirmed multi-country use case.
- [ ] **WhatsApp Business inbound + outbound.** WhatsApp Business API approval + template messaging. Same classifier as email/SMS. Australia-heavy tenant population would benefit. Meta approval gates this.
- [ ] **Voice / phone call intake.** Twilio Voice + OpenAI Whisper transcription. Inbound tenant call becomes a transcript + AI summary on the tenant timeline. Big bet; needs operator workflow design.
- [ ] **Single-click property advertising.** One operator action on a vacant property assembles the full listing — AI vacancy-marketing copy + selected property photos + price-band recommendation — previews it, and on a second explicit approval publishes to the AU portals, then routes responses into applicant triage. This is the operator-facing umbrella over three pieces already in this list: **vacancy marketing copy generation** (the draft), **listing-platform integrations (realestate.com.au + Domain)** (the publish), and **vacancy applicant triage** (the inbound). Review-first per [`CLAUDE.md`](../CLAUDE.md): the click assembles and previews; the portal publish is an explicit operator approval, never automatic. Blocked-by: those three dependencies landing first (esp. listing-platform partner access) + market-rate data for the price band. Effort: small once they exist — mostly the one-click orchestration + a preview/approve surface.
- [ ] **Vacancy marketing copy generation.** Vacant property → AI drafts listing copy + price recommendation + photo selection. Requires market-rate data integration (RP Data / CoreLogic) for the price band.
- [ ] **Vacancy applicant triage.** Applications arrive via listing platforms (realestate.com.au / Domain) → AI summarises affordability + references + flags → operator approves the shortlist. Needs listing-platform integrations.
- [ ] **Predictive maintenance.** Pattern recognition over inspection + work-order history. Suggests preventive jobs before they become reactive. Needs years of internal data to train usefully.
- [ ] **Year-end tax pack generation.** Compiles annual statements for each owner. Touches tax accounting; needs accountant sign-off on the format.
- [ ] **Council / statutory communication routing.** Inbound council letters → AI classifies + routes to the right property/lease.
- [ ] **Tenant self-serve rent adjustment.** Tenant requests rent relief via the portal → AI summarises affordability evidence → operator approves.
- [ ] **Automated lease termination workflow.** Lease end-of-term → AI runs bond release → final account → tenant exit communications. Operator approves each stage.

### Pre-existing backlog (still valid, lower priority)

- [ ] Apply the new `20260520_0018_maintenance_arrears_foundations`, `20260520_0019_tenant_portal_accounts`, `20260521_0020_register_import_plans`, `20260521_0021_operator_notification_preferences`, and `20260522_0022_branded_communication_templates` migrations in hosted Neon/Render if auto-migrations do not run.
- [ ] Remba review the new Smart Intake spreadsheet import panel, simplified navigation/command center, Portfolio QA command-search placement, reusable evidence/source-trail pattern across Properties/Smart Intake/Tenants, tenant portal, tenant fresh-link recovery, invoice email action, Work assignment controls/workload/reminder/action/provider notice cues, and Operations workspace before treating them as UX complete.
- [ ] Live-review the Operations row-density pass on a real phone and tune disclosure labels if they feel unclear.
- [ ] Run the live MVP performance audit after deployment using `npm run audit:live -- --login`, then `LEASIUM_AUDIT_URL=https://leasium.ai npm run audit:live`; use the report to decide whether the next snappiness work is Render plan/region, Neon region/pooling, Redis/summary caching, or endpoint-level SQL aggregation.
- [~] Add production observability before broad MVP traffic: Vercel Speed Insights web-vitals capture is mounted in the web root layout via `@vercel/speed-insights/next`, backend Sentry scaffolding is guarded behind `SENTRY_DSN`, and the API emits `server-timing` plus `x-request-id` headers for request-duration review. Live Speed Insights data still needs the next Vercel deployment plus project/dashboard confirmation; no tenant, document, provider payload, or business event data is sent through the web-vitals path.
- [x] Continue Xero from guided sync exceptions into accounting snapshot guardrails, stale reconciliation indicators, and richer accounting-readiness snapshots. Local freshness snapshots, Billing Readiness row cues, month-end checklist, owner-statement handoff, and Settings next-step guidance are shipped; remaining follow-up is live SKJ/accountant tuning.
- [x] Continue Portfolio QA from guided cleanup v1 into bulk fix review, AI-assisted enrichment candidates, and clearer completion/reporting states. Completion report, cleanup report CSV download, enrichment candidate queue, reviewed bulk-staging actions, blocked-followup queue, onboarding/billing blocker-review summaries, and final-readiness verdicts are shipped; remaining follow-up is live SKJ tuning.
- [x] Smart Intake spreadsheet migration template download shipped: the spreadsheet import section now has a `Download template` action, and the generated workbook covers the SaaS/bulk migration fields through the current import tabs: Entities for owners/billing identities, Properties, Tenancies for units/tenants/leases, Charge Rules, Bonds/Dates for obligations and compliance, Vendors for contacts/vendors, Arrears, Active Issues, and Actions. Backend template/download coverage and spreadsheet smoke have passed; remaining follow-up is live SKJ/Remba tuning of sheet names, field order, and whether additional AI-fill instructions should sit in-app or only inside the workbook.
- [~] Branded communications editable templates UI + send-time wiring (deprioritised while SKJ uses Leasium internally per `leasium-internal-first-6-months`). Read-only stored override visibility is now shipped, including a local `communication-template-overrides.csv` export from Settings for runtime template keys, stored override names/versions/providers, active/system/override state, coverage status, and no-send/no-mutation guardrails. Remaining work is actual editing and provider send-time consumption after Remba/operator review.

## Smart Intake North Star

Smart Intake is the primary Leasium workflow: users drag documents into the platform and Leasium creates a reviewable work item. The system should classify the file, extract structured facts, match existing records, highlight proposed changes, show confidence and blockers in plain English, then apply updates only after approval. Manual forms stay available, but the default path is upload, review, approve.

Target document jobs:

- New building or acquisition: purchase contract, disclosure pack, title, tenancy schedule, and management handover files become property, unit, tenant, lease, document, and obligation drafts.
- New tenant lease: lease PDF/DOCX becomes tenant, lease, rent, option, security, outgoings, insurance, and critical date drafts.
- Tenant onboarding: insurance certificates, guarantees, and supporting documents attach to the onboarding and update expiry/security fields after review.
- Billing/admin: invoices, rent schedules, outgoings budgets, and Xero-ready details become invoice or charge-rule drafts.
- Compliance: certificates, fire/safety reports, insurance renewals, and inspection reports become document records plus recurring obligations.

## Re-Leased Benchmark

Official Re-Leased pages position their product around a few mature modules that should guide Leasium's backlog:

- Xero/accounting: two-way sync for contacts, invoices, bills, credit notes, accounts, tracking categories, and tax rates, with Re-Leased handling lease logic and Xero handling accounting. Source: https://www.re-leased.com/product/xero
- Tenant portal: tenants can view lease details, invoices, documents, rent status, maintenance, compliance, and payment history from a browser portal. Source: https://www.re-leased.com/tenant-portal
- Maintenance and compliance: work orders, contractor coordination, preventive tasks, inspections, audit trails, and lease-driven obligations. Source: https://www.re-leased.com/product/property-operations-maintenance
- Insights and shareable snapshots: live dashboards should come first, with owner and finance snapshots generated from the same lease performance, financial operations, arrears, maintenance, and portfolio analytics data. Source benchmark: https://www.re-leased.com/product/property-management-reporting
- API/integrations: REST API coverage for properties, leases, contacts, invoices, maintenance, inspections, dashboards, and communication tools. Source: https://www.re-leased.com/product/apis

## Later Modules

- [ ] Maintenance and arrears workflow depth: tenant-submitted requests, contractor coordination, approval history, invoice linking, legal/escalation notes, and portal-visible status where appropriate.
- [ ] Compliance and inspections: recurring checks, certifications, document expiry, fire/safety obligations, delegated owners, and audit trail.
- [ ] Tenant portal account lifecycle: invite renewal handling, tenant-side account recovery, multi-login management, and change-email support.
- [ ] Portfolio Insights: rent roll health, lease events, arrears, invoice status, maintenance aging, compliance expiry, owner/entity dashboards, and shareable snapshots.
- [ ] Communications hub: email templates, outbound logs, tenant/contractor threads, and record-linked correspondence.
  - [x] Tenant correspondence timeline v1: tenant detail now surfaces a read-only Correspondence panel from stored inbound messages and reviewed comms audit receipts, with guardrails that opening the panel never sends providers or mutates queue/tenant state. Broader hub threads/templates/log workflows remain open.
  - [x] Tenant correspondence CSV export v1: tenant detail can download a local review-only correspondence CSV from the already-loaded timeline, including inbound/dispatch rows, target context, endpoint guardrails, and a no-send/no-mutation export guardrail.
  - [x] Tenant correspondence record links v1: tenant correspondence rows now expose local open-record links for linked arrears, maintenance, inbound comms, tenant workflow, and obligation targets so the timeline can hand operators back into the source work without sending or mutating anything.
  - [x] Comms outbound log v1: the Comms hub now shows a compact read-only dispatch receipt log from stored comms audit rows, with local target links, CSV export, and guardrails that viewing or exporting the log never sends, dismisses, refreshes providers, or mutates queue state.
  - [x] Comms outbound log filters v1: the Comms hub dispatch receipt log now has compact all/needs-attention/email/SMS filters, filtered row counts, and filtered CSV export scope while keeping the log read-only and provider-mutation safe.
  - [x] Maintenance correspondence panel v1: maintenance work-order detail now shows linked Comms dispatch/dismiss receipts for tenant and contractor forwards, with local Comms/tenant handoffs, CSV export, and guardrails that the panel never sends, dismisses, refreshes providers, or mutates the work order.
- [ ] Approvals and workflow: invoice approval, maintenance spend approval, delegated tasks, reminders, escalation rules, and activity history.
- [ ] Mobile/field workflows for inspections, photos, quick notes, and work order updates.

## UX Principles

- Remba signs off any design-facing navigation, layout, workflow, copy, or visual hierarchy change before it is treated as complete.
- Keep first actions obvious: upload a lease, add a property, invite a tenant, add a charge.
- Prefer document-first workflows over manual field entry whenever a source document exists.
- Every extracted field needs provenance: which document, where it came from, confidence, and what will change.
- Prefer review screens and status panels over hidden configuration.
- Make blockers plain English, with the next corrective action beside the blocker.
- Avoid forcing users through accounting concepts until they are needed for invoice readiness.
- Every module should reduce a spreadsheet, email chase, or duplicate entry job.
- Prefer live dashboards and exception queues over static report pages; exports should be generated from dashboards when needed.
